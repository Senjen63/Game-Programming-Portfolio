/*******************************************************
* Author: Aven Presseisen
* Class: GPR-250-02
* Assignment: Assignment 3
* Certification of Authenticity:
* I certify that this assignment is entirely my own work.
************/

#pragma once

#include <vector>

#include <Vector2D.h>

#include "Animation.h"
#include "GraphicsSystem.h"

class Unit : public Trackable
{
	Vector2D mPosition;
	std::vector<Animation *> mAnimations;
	int mCurrentAnimation = -1;

public:
	Unit();
	~Unit();

	void addAnimation( Animation * pAnimation );
	void draw( GraphicsSystem * pGfxSystem );
	int getCurrentAnimationIndex() const;
	Vector2D getPosition();
	Vector2D getSize();
	bool isOnPosition( Vector2D pos );
	void setAnimation( int animation );
	void slowDownAnimation();
	void speedUpAnimation();
	void updateUnit();
	void updatePosition( Vector2D newPos );
};
