#pragma once

#include <PerformanceTracker.h>
#include <MemoryTracker.h>
#include <Vector2D.h>

#include "GraphicsBuffer.h"

using namespace std;

class Sprite : public Trackable
{
	friend class GraphicsSystem;
	friend class GraphicsBuffer;
	friend class Animation;

public:
	Sprite(GraphicsBuffer* graphicsBuffer, Vector2D location, int height, int width);
	Sprite(GraphicsBuffer* graphicsBuffer, Vector2D location);
	~Sprite();

	int getHeight();
	int getWidth();
	Vector2D getLocation();
	GraphicsBuffer* getBuffer();

private:
	//Variables
	int mHeight;
	int mWidth;
	//Location of the upper left corner of the sprite.
	Vector2D mLocation;
	GraphicsBuffer* mpGraphicsBuffer;
};