#pragma once
#include <vector>

#include "Unit.h"
#include "SnakeManager.h"

class UnitManager
{
public:
	UnitManager();
	~UnitManager();

	void addUnit(std::pair<float, float>location, Animation* currentAnimation);
	void addUnit(Unit* addedUnit);
	void removeUnit(int unitIndex); // Removes unit based on given index
	void removeUnit(Unit* targetUnit); // Removes unit based on given pointer
	void removeAllUnits();
	Unit* getUnit(int unitIndex); // Gets unit based on given index
	Unit* getUnit(std::pair<float, float>location); // Gets unit based on given position
	int getTotalUnits() { return mUnitVector.size(); }

	void updateAll(float deltaTime);
	void drawAll(GraphicsSystem* currentGraphicsSystem);

private:
	std::vector<Unit*> mUnitVector; // Should I be using a vector of pointers?
};
