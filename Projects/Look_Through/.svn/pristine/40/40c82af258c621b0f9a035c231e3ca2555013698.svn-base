#pragma once

#include <Trackable.h>
#include "Vector2D.h"

class Sprite;
class Font;
class Color;

using namespace std;

class HUD : public Trackable
{
	private:
		Font* mpFont;
		Color* mpTextColor;

		string mFontFileName;
		int mFontSize = 0;
		string mTextAlignment;

		string mInstructionText;
		string mLevelText;
		string mScoreText;
		string mFpsText;
		string mTimeText;

		Vector2D mInstructionLocation;
		Vector2D mGameInfoLocation;
	public:
		HUD();
		~HUD();

		void drawHud(double);
};