#pragma once

#include <string>
#include "GameEvent.h"

using namespace std;

class LoseEvent : public GameEvent
{
public:
	LoseEvent(const int& status);
	~LoseEvent();

	inline int getStatus() const { return mStatus; }

private:
	int mStatus;
};