#pragma once

#include <EventListener.h>

enum class KeyCode;

class EventSystem;

class InputTranslator : public EventListener
{
public:

	InputTranslator();
	~InputTranslator();

	void handleEvent(const Event& theEvent);

private:

	void parseKeyEvent(KeyCode key);

	EventSystem* mpEventSystem = nullptr;

};

