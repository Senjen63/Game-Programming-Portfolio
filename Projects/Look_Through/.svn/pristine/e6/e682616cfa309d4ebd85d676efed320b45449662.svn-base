#pragma once
#include "GraphicsBuffer.h"
#include "Font.h"
#include "Color.h"
#include "GraphicsBufferManager.h"

class GraphicsSystem;

const int FONT_SIZE = 20;
const int BUFFER_WIDTH = 800;
const int BUFFER_HEIGHT = 35;

const string BLACK_KEY = "Black";

class HUD : public Trackable
{
private:

	Font* mpFont = nullptr;

	GraphicsBufferManager mBufferManager;
	//GraphicsBuffer* mBlackBackground = nullptr;

	float mFPS, mTime;

public:
	HUD();
	~HUD();

	void init(Font* font);
	void cleanup();

	void drawHUD(GraphicsSystem* display);	
};
