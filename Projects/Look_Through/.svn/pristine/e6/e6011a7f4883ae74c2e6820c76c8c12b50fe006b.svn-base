//BP
#pragma once
#include "HUDEvents.h"
#include "GameEvents.h"

enum SpawnEventVariations
{
    WALL_SPAWN = NUM_HUD_EVENT_TYPES,
    SNAKE_SPAWN,
    FOOD_SPAWN,
    ITEM_SPAWN,
    NUM_SPAWN_EVENT_TYPES
};

class SpawnEvents : public GameEvents
{
private:
public:
    SpawnEvents(SpawnEventVariations tag);
    ~SpawnEvents();
};
