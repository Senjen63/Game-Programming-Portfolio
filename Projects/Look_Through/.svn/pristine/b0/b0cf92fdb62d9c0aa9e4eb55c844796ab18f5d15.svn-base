#pragma once

#include "GameEvent.h"
#include "Unit.h"

class CollisionEvent : public GameEvent
{
public:
    CollisionEvent(Unit*, Unit*);
    ~CollisionEvent();

    Unit* getChecked() const { return mChecked; }
    Unit* getCollider() const { return mCollider; }
private:
    Unit* mChecked = nullptr;
    Unit* mCollider = nullptr;
};