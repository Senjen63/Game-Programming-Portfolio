#pragma once
#include "Trackable.h"
#include "GraphicsBuffer.h"
#include <map>

class GraphicsBufferManager : public Trackable
{
	//could possibly make this a friend of graphicsbuffer, might be heavy handed. 
private:
	map<string, GraphicsBuffer*> mpGraphicsBuffers;
public:
	GraphicsBufferManager() {};
	~GraphicsBufferManager() { removeAllBuffers(); };

	void createBuffer(string buffName ,string path);
	void createBuffer(string buffName, Color color, int height, int width);
	void createBuffer(string buffName, ALLEGRO_BITMAP* pBitmap);

	GraphicsBuffer* getBuffer(string buffName) { return mpGraphicsBuffers[buffName]; };
	void addBuffer(string buffName, GraphicsBuffer* buff) { mpGraphicsBuffers[buffName] = buff; };
	void delBuffer(string buffName);

	void removeAllBuffers();


	/* GraphicsBuffer(string path);
	GraphicsBuffer(Color color, int height, int width);
	GraphicsBuffer(ALLEGRO_BITMAP* pBitmap); */
};