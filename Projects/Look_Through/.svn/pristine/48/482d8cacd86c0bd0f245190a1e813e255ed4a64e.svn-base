//BP
#pragma once
#include "Font.h"
#include "GameHUD.h"
#include "GameTime.h"
#include "HUDEvents.h"
#include "Framerate.h"
#include "GameScore.h"
#include <EventSystem.h>
#include <EventListener.h>
#include "GraphicsSystem.h"
#include "GraphicsBuffer.h"

const int DEFAULT_PANEL_WIDTH = 100;
const int DEFAULT_PANEL_HEIGHT = 75;

class GameHUD : public EventListener
{
private:
    int mHori;
    int mVert;
    float mFramerate;
    int mPlayerScore;
    float mTimePassed;
    Font* mpFont = nullptr;
    EventSystem* mpEventSystem = nullptr;
    GraphicsBuffer* mpDisplayTime = nullptr;
    GraphicsBuffer* mpDisplayScore = nullptr;
    GraphicsSystem* mpGraphicSystem = nullptr;
    GraphicsBuffer* mpDisplayFramerate = nullptr;
public:
    GameHUD();
    GameHUD(int hori, int vert, GraphicsSystem* graphics, Font* font);
    ~GameHUD();
    void init();
    void cleanup();
    void showHUD();
    virtual void handleEvent(const Event& thisEvent);
};