#pragma once
#include <Trackable.h>
#include <iostream>
#include "Sprite.h"
#include "Font.h"

class GraphicsSystem : public Trackable 
{
private:
	ALLEGRO_DISPLAY* mpDisplay;
	GraphicsBuffer* mpBackBuff;
public:
	GraphicsSystem();
	GraphicsSystem(int width, int height);
	~GraphicsSystem();
	GraphicsBuffer* getBackBuff();
	int getWidth();
	int getHeight();
	bool init();
	void cleanup();
	void flip();
	bool saveBuff(string file, GraphicsBuffer* buff);
	void draw(int hori, int vert, Sprite sprite, float horiScale, float vertScale);
	void draw(GraphicsBuffer* pBackBuff, Sprite sprite, int hori, int vert, float horiScale, float vertScale);
	void draw(GraphicsBuffer* pBackBuff, int hori, int vert, float horiScale, float vertScale);
	void displayMessage(int hori, int vert, string message, Font* font, Color color, GraphicsBuffer* pBackBuff);
	void displayMessage(int hori, int vert, string message, Color color, Font* font);
};