#pragma once

#include "SoundBuffer.h"

#include <Trackable.h>

class Sound : public Trackable
{
public:
	Sound(SoundBuffer* baseBuffer);
	~Sound();

	void play() { mSound->play(); }
private:
	SoundBuffer* mBuffer;
	sf::Sound* mSound;
};