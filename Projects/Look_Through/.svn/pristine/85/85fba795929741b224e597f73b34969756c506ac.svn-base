#pragma once

#include "Trackable.h"
#include <allegro5/allegro.h>

class Color : public Trackable
{
private:

	ALLEGRO_COLOR mColor;

public:

	Color(int redValue, int greenValue, int blueValue, int alphaValue);
	~Color() {};

	ALLEGRO_COLOR getColor() { return mColor; }
};
