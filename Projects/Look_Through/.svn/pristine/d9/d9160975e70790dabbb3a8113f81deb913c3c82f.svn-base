#pragma once

#include <PerformanceTracker.h>
#include <MemoryTracker.h>
#include "Animation.h"
#include <Vector2D.h>

class Unit : public Trackable
{
public:
    Unit();
    Unit(Animation* one, Animation* two, Vector2D loc);
    ~Unit();

    void Update(float dt);

    void Draw(GraphicsSystem* pGSys, Vector2D location);

    Sprite Draw();

    void swapAnim();
    int getCurrentSprite();

    float getTiming();
    void changeFrameRate(float speed);

private:
    Animation* mCurrentAnim;
    Animation* mAltAnim; 
    Vector2D location;

};