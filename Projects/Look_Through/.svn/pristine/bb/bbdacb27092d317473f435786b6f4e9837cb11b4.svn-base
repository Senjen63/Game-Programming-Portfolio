#pragma once

#include "GraphicsSystem.h"
#include <Trackable.h>

class InputSystem : public Trackable
{
public:
	InputSystem();
	~InputSystem();
	bool init();
	void cleaner();
	Vector2D getMouse(int mouseButton);
	bool getKeyPress(int key);
	bool getMouseButtonPress(int mouseButton);
private:
	bool mIsClean = false;
	bool mIsInit = false;
};