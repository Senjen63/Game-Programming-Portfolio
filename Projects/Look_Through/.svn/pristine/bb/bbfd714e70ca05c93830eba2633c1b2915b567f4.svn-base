#pragma once
#include "UnitManager.h"

const int BABB_SIZE = 64;

class BabbManager : public UnitManager
{
public:
	BabbManager();
	~BabbManager();

	void moveBabb(float dt);
	void setDirection(int dir);
	bool attackTobb(Vector2D pos);

private:
	bool mRight = true;
	bool mLeft = false;
	float mBabbTimePerFrame = 1000.0 / 2;
};
