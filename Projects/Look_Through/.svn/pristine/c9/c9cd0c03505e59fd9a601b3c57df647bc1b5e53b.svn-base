#pragma 
#include <Trackable.h>
#include "Animation.h"
#include "GraphicsSystem.h"

//holds state information for a gmae entity: holds location info for an animation
class Unit: public Trackable
{
private:
	Vector2D mLocation;
	bool mSmurf;
	Animation mpAniSmurf;
	Animation mpAniDean;

public:
	Unit();
	Unit(Animation aniSmurf, Animation aniDean);
	~Unit();

	void update(double dt);//adjust state of Unit: call Animation's update function
	void draw(GraphicsSystem*&);//draw the current animation's current sprite in location of unit
	void setAnimation();//switch to new animation
	void setSpeed(int speed);
	void setLocation(Vector2D location);

	float getSpeed();
};