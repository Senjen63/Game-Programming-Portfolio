#include "Sound.h"
#include <assert.h>

using namespace std;

Sound::Sound(SoundBuffer* baseBuffer)
{
	mBuffer = baseBuffer;
	mSound = new sf::Sound();

	mSound->setBuffer(*(mBuffer->mBuffer));
}

Sound::~Sound()
{
	delete mSound;
}