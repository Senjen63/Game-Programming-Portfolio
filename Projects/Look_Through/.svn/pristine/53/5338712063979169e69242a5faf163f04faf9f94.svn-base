#include <iostream>
#include <cassert>
#include <string>

#include <PerformanceTracker.h>
#include <MemoryTracker.h>
#include <Vector2D.h>
#include <Timer.h>

#include "Game.h"

using namespace std;

int main()
{
	gpGame = new Game(DISP_HEIGHT, DISP_WIDTH, TARGET_FRAMETIME);
	gpGame->init();

	gpGame->doLoop();

	gpGame->deinit();

	delete gpGame;
	gpGame = nullptr;

	MemoryTracker::getInstance()->reportAllocations(std::cout);
	system("PAUSE");
}
