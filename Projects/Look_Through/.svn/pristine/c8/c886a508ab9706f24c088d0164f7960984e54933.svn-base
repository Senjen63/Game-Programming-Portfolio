#include "Music.h"
#include <assert.h>

using namespace std;

Music::Music(string filename, bool looping)
{
	mMusic = new sf::Music();

	assert(mMusic->openFromFile(filename));

	mMusic->setLoop(looping);
}

Music::~Music()
{
	delete mMusic;
}