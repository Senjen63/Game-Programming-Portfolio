#pragma once

#include "EventListener.h"
#include "GraphicsBufferManager.h"
#include "Font.h"
#include "Color.h"

class HUD : public EventListener
{

public:
	HUD(GraphicsBufferManager* buffers);
	~HUD();

	void handleEvent(const Event& theEvent);
	void setFPS(double newFPS) { mFPS = newFPS; }

	void update();
	void draw();

	const static int LEADING_SPACE = 20;
	const static int MS_TO_SECONDS = 1000;
	const static int SECONDS_TO_MINUTES = 60;

	const int FONT_SIZE = 48;
private:
	int mCurrentScore;
	int mCurrentMinutes;
	int mCurrentSeconds;
	int mCurrentFrames;
	double mFPS;

	Color mCol;
	Font* mFont;

	GraphicsBufferManager* mBuffers;
};
