#include "Font.h"

Font::Font(std::string fontLocation, float fontSize, int red, int blue, int green)
{
	mpFont = TTF_OpenFont(fontLocation.c_str(), fontSize);
	mFontSize = fontSize;

	mColor.r = red;
	mColor.b = blue;
	mColor.g = green;
}

Font::~Font()
{
	// unload font?
}