#pragma once
#include <PerformanceTracker.h>
#include <MemoryTracker.h>
#include <Trackable.h>
#include "Animation.h"
#include "Vector2D.h"

using namespace std;

class Unit : public Trackable
{
public:
	Unit();
	Unit(Vector2D position, Animation* pMainAnim, Animation* pAltAnim);
	~Unit();
	void update(double dt);
	float getTime();
	void setTime(float timeChange);
	void setAnimation();
	void setPosition(float x, float y);
	Vector2D getPosition();
	Sprite* draw();
private:
	Animation* mpMainAnim;
	Animation* mpAltAnim;
	int mCurrentAnim;
	Vector2D mPosition;
};