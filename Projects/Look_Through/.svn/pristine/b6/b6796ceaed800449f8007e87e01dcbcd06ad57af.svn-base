#include "Font.h"

Font::Font(std::string name, int size)
{	
	mSize = size;

	mpFont = TTF_OpenFont(name.c_str(), size);

	if (!mpFont)
	{
		printf("Failed to load lazy font! SDL_ttf Error: %s\n", TTF_GetError());
	}
}

Font::~Font()
{
	TTF_CloseFont(mpFont);
	
	mpFont = NULL;
}