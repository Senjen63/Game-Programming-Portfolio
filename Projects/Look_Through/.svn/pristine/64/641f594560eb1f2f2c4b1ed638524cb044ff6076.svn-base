#pragma once

#include "Trackable.h"
#include "Animation.h"

class Wall : public Trackable
{
public:
	Wall();
	Wall(int x, int y, Animation* anim);
	~Wall();

	int getX() { return mX; }
	int getY() { return mY; }

	void update(float elapsedTime);
	void draw();
private:
	int mX;
	int mY;
	Animation* mAnimation;
};
