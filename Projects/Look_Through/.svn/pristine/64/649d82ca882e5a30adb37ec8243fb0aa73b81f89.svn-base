#pragma once

#include "Unit.h"
#include <vector>
#include <Trackable.h>
#include <Vector2D.h>

using namespace std;

class UnitManager : public Trackable
{
public:
	UnitManager();
	~UnitManager();

	void cleanup();

	Unit* getLast();
	int getUnitNum();

	Unit* addUnit(Vector2D, int);
	void deleteUnit(Unit*);
	void deleteUnits(vector<Unit*>*);
	void deleteUnitsAtPosition(Vector2D mousePosition);
	void clearAll();

	void nextAnimForUnit();

	void setAllPlay(bool);
	void switchAllPlay();

	void updateAll(float);
	void drawAll();

	Unit* clickedOn(Vector2D);
	vector<Unit*>* clickedUnits(Vector2D);

private:
	vector<Unit*> mpUnits;
};