#pragma once

#include "BoxCollider.h"
#include "CircleCollider.h"

class CollisionMath
{
public:
    static bool boxOnBox(const BoxCollider& box1, const BoxCollider& box2);
    static bool boxOnCircle(const BoxCollider& box, const CircleCollider& circle);
    static bool circleOnCircle(const CircleCollider& circle1, const CircleCollider& circle2);
};
