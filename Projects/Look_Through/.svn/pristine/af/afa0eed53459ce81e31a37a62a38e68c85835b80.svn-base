//BP
#pragma once
#include <Vector2D.h>
#include "GameEvents.h"

class Movement : public GameEvents
{
public:
    Movement(Vector2D position);
    ~Movement();
    Vector2D getMovementPosition() const { return mPosition; }
private:
    Vector2D mPosition = Vector2D(0, 0);
};