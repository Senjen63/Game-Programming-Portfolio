#pragma once

#include <string>
#include <trackable.h>
#include "LevelEvent.h"

using namespace std;

enum InputEventType
{
	LEFT_EVENT = NUM_BASE_EVENT_TYPES,
	RIGHT_EVENT,
	UP_EVENT,
	DOWN_EVENT,
	CONTINUE_EVENT,
	QUIT_EVENT
};

class InputEvent : public LevelEvent
{
public:
	InputEvent(InputEventType type);
	~InputEvent();

	InputEventType getInput() const { return mType; }
private:
	InputEventType mType;
};