#pragma once
#include "Audio.h"

class AudioSystem : public Trackable
{
private:

public:

	bool init();
	void cleanUp();

	AudioSystem() {};
	~AudioSystem() { cleanUp(); };

	void playLoopingMusic(Audio* audio);
	void playSound(Audio* audio);
	void mute(bool isMute, Audio* audio);

};
