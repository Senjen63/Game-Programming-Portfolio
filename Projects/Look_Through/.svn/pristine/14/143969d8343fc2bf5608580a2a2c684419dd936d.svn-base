#include "Color.h"

using namespace std;

Color::Color()
{
	mRed = MIN_VALUE;
	mGreen = MIN_VALUE;
	mBlue = MIN_VALUE;
	mAlpha = MAX_VALUE;
}

Color::Color(int red, int green, int blue, int alpha)
{	
	mRed = red;	
	mGreen = green;
	mBlue = blue;
	mAlpha = alpha;
}
