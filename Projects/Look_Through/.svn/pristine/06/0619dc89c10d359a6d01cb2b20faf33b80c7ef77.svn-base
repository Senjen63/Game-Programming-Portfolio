#pragma once
#include "Unit.h"

class UnitManager : public Trackable
{

	friend class Snake;

public:
	UnitManager();
	~UnitManager();
	void addUnit(Sprite smurf, Vector2D pos);
	void addUnit(int x, int y, Animation* smurf);
	void addUnit(int x, int y, Animation* smurf, Animation* dean);
	void removeUnit();
	Unit* getUnit(int unitSpot);
	int getSize();
	void clearAll();
	void updateAll(float removeTime);
	void drawAll(GraphicsSystem* display);
	void drawAll(GraphicsSystem* display, Vector2D pos);


private:
	
	vector<Unit*> mUnitList;
	int mSize;

};
