#pragma once

#include <unordered_map>
#include "AnimationPrototype.h"

typedef std::string AnimKey;

class AnimationManager : public Trackable
{
public:
	AnimationManager() {}
	~AnimationManager();

	void cleanup();

	void createAndAddAnimation(const AnimKey& key, GraphicsBuffer* proto, int type);
	void destroyAnimation(const AnimKey& key);

	AnimationPrototype* getAnimation(const AnimKey& key) const;

private:
	std::unordered_map <AnimKey, AnimationPrototype*> mMap;
};
