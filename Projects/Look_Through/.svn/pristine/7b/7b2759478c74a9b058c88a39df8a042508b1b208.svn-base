#pragma once
#include "Unit.h"
#include <unordered_map>
#include <vector>
#include <Trackable.h>

class UnitManager : public Trackable
{
private:
	vector<Unit> mUnits;

public:
	UnitManager();

	~UnitManager();

	void setAnimation();

	void addUnit(Unit unit);

	void removeUnit(Vector2D location);

	Unit &getUnit(int location);

	Unit &getRecentUnit();

	int getLength();

	void removeAll();

	void updateAll(double dt);

	void drawAll(int scale);

	bool isEmpty();
};