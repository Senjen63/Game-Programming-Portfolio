/*******************************************************
* Author: Aven Presseisen
* Class: GPR-250-02
* Assignment: Assignment 1
* Certification of Authenticity:
* I certify that this assignment is entirely my own work.
************/

#pragma once

#include "GraphicsBuffer.h"

class Sprite : public Trackable
{
	GraphicsBuffer * mpGraphicsBuffer;
	int mLOCX;
	int mLOCY;
	int mHeight;
	int mWidth;

public:
	Sprite( GraphicsBuffer & graphicsBuffer, int locX, int locY, int width, int height );
	Sprite( Sprite && src ) noexcept;
	~Sprite();

	// Accessors
	const GraphicsBuffer & getGraphicsBuffer() const;
	// These aren't alphabetical simply because they are pairs
	int getLOCX() const;
	int getLOCY() const;
	int getHeight() const;
	int getWidth() const;
};
