#pragma once
#include "Trackable.h"
#include "Vector2D.h"
#include <allegro5/allegro.h>

class InputSystem : public Trackable
{
public:
	InputSystem() {};
	~InputSystem() {};
	bool init();
	void cleanUp();
	bool getKeyState(int which_key);
	int getMouseState();
	Vector2D getMousePosition();
private:
	//Do this for each input necessary.
};