#pragma once
#include <MemoryTracker.h>
#include <PerformanceTracker.h>

#include "Sprite.h"
class Animation : public Trackable
{
private:
	float mTime;
	float mCurrentTime;
	int mCurrentSprite;
	int mNumSprites;
	bool mLoop;
	Sprite* mpSpritesheet;
	int currIncrement = 0;

public:
	Animation();
	Animation(GraphicsBuffer* pBuffer, int numSprites, float timing, bool loop);
	~Animation();

	void Update();

	Sprite* GetCurrentSprite();

	void AdjustTime(float modifier);
	float GetTime();
};