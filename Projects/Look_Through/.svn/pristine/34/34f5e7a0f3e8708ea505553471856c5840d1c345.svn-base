#pragma once

#include <Trackable.h>
#include <vector>
#include "Sprite.h"

const int MAX_NUM_OF_SMURFS = 16;

class Animation : public Trackable
{
	private:
		std::vector<Sprite> mSmurfSprites;
		float mTimePerFrame;
		float mTimeLeft;
		int mCurrentSprite;

	public:
		Animation();
		~Animation();
		void addSprite(GraphicsBuffer* buffer, int x, int y, int height, int width);
		void update(double dt);
		Sprite getCurrentSprite();
		void speedUp();
		void slowDown();
};