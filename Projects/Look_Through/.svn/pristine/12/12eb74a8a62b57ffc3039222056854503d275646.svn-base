#pragma once

#include "LevelEvent.h"
#include "Powerup.h"

class CollectEvent : public LevelEvent
{
public:
	CollectEvent(int id, PowerupType type);
	~CollectEvent();

	const int getID() const { return mID; };
	const PowerupType getType() const { return mType; }

private:
	PowerupType mType;
	int mID;
};