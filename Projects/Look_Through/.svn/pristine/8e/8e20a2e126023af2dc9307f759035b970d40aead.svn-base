#pragma once
#include "Button.h"

// Enum for different option types?
// Fire event corresponding to updated option type with information corresponding to new value?

class OptionsButton : public Button
{
public:
	OptionsButton(std::pair<float, float>location, std::pair<float, float>scale, string text, string animationDataFilePath, string updatedOption, string newOptionValue);
	virtual ~OptionsButton();

	virtual void onClicked();

private:
	// Change to enum later?
	std::string mUpdatedOption;
	std::string mNewOptionValue;
};