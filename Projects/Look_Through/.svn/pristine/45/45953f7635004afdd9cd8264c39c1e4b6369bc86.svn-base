//Evan Norzow, Nicolas Delbue, and Brandon Perry
#pragma once
#include <string>
#include <fstream>

using namespace std;

class SaveData
{
public:
	SaveData();
	~SaveData();

	void SaveTheData(int, int, int);
	void LoadData();
	void ResetData();

	int GetPlayerOneScore();
	int GetPlayerTwoScore();
	int GetDiff() { return mDiff; };

private:
	int mPlayerOneScore = 0, mPlayerTwoScore = 0, mDiff;
	const string fileName = "saveInfo.txt";
};
