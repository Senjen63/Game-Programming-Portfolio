#pragma once

#include <string>
#include "GameEvent.h"

using namespace std;

class InputEvent : public GameEvent
{
	public:
		InputEvent(const string& releaseState, const int& key);
		~InputEvent();

		inline string getReleaseState() const { return mReleaseState; }
		inline int getKey() const { return mKey; };

	private:
		string mReleaseState;
		int mKey;
};