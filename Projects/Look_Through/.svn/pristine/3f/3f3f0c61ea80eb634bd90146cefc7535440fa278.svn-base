#include "SDL_FILES.h"
#include <PerformanceTracker.h>
#include <vector>
#include "Vector2D.h"
#include "Color.h"
#include "GraphicsSystem.h"

using namespace std;

class Font;
class Color;
// location font color string
class Menu : public Trackable
{
public:
	Menu();
	Menu(GraphicsSystem* pSystem, Font* pFont);
	~Menu();
	void addLine(Vector2D pos, string text);
	void writeAll();
	void writeLines(int startLine, int numLines);
	int getLineCount();
	void extendLine(int pos,string text);
private:
	int mNumLines = 0;
	vector<Vector2D> mLineLocations;
	vector<string> mText;
	Font* mpFont = nullptr;
	Color mWhite = Color(255, 255, 255);
	Color mBlack = Color(0, 0, 0);
	GraphicsSystem* mpGraphicsSystem = nullptr;

};