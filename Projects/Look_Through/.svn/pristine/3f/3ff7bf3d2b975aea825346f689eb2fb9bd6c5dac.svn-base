#pragma once
#include <PerformanceTracker.h>
#include <MemoryTracker.h>
#include <Vector2D.h>

#include "Color.h"
#include "Font.h"

class HUD : public Trackable
{
public:
	HUD();
	HUD(string message, Vector2D pos);
	~HUD();

	void update(Font* pFont, int score, float FPS, float time);
	void update(Font* pFont);

private:
	Color mpColor;
	Vector2D mpScoreLocation, mpTimeLocation, mpFPSLocation;
	string mMessage;
};