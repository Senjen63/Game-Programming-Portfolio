#pragma once

#include <PerformanceTracker.h>
#include <MemoryTracker.h>

#include "GraphicsBuffer.h"

class Sprite : public Trackable
{
private:
	GraphicsBuffer* mpGraphicsBuffer;
	int mX, mY, 
		mWidth, mHeight;

public:
	Sprite();
	Sprite(GraphicsBuffer* graphicsBuffer, int x, int y, int width, int height);
	~Sprite();

	GraphicsBuffer* getBuffer();
	int getX();
	int getY();
	int getWidth();
	int getHeight();
};