#pragma once

#include <allegro5/allegro.h>
#include <allegro5/allegro_font.h>
#include <allegro5/allegro_ttf.h>
#include <allegro5/allegro_audio.h>
#include <allegro5/allegro_acodec.h>
#include <allegro5/allegro_image.h>
#include <allegro5/allegro_primitives.h>

#include <iostream>
#include <cassert>
#include <string>

#include "color.h"

using namespace std;

class GraphicsBuffer //Allegro Bitmap, height width of bitmap, load and create bitmap
{
	friend class GraphicsSystem;
	//Constructors for loading file, or creating it some with colors
	//Destructor 1
	//Accessors for height width other vars
public:
	GraphicsBuffer();
	GraphicsBuffer(string& fileName);
	GraphicsBuffer(Color color, int width, int height);

	~GraphicsBuffer();

	float GetHeight();
	void SetHeight(float);
	float GetWidth();
	void SetWidth(float);

	ALLEGRO_BITMAP* GetBitmap();
	void SetBitmap(ALLEGRO_BITMAP* newBitmap);

private:
	GraphicsBuffer(ALLEGRO_BITMAP* pBitmap);

	ALLEGRO_BITMAP* mpBitmap;
	float mHeight, mWidth;
	bool mOwns;
};