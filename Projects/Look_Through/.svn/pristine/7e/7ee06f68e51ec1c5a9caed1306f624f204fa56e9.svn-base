#pragma once

#include "Unit.h"
#include "GameEvent.h"
#include "EventSystem.h"

class PowerUpEvent : public GameEvent
{
public:
	PowerUpEvent(Unit* powerUpObject, Unit* snakeObject, int powerUpType);
	virtual ~PowerUpEvent();

	Unit* getPowerUp() const { return mpPowerUpObject; }
	Unit* getSnake() const { return mpSnakeObject; }
	float getPowerUpType() const { return mPowerUpType; }

private:
	//const Unit mpInstance; // Objects stored in events still listen for events dont they
	Unit* mpSnakeObject;
	Unit* mpPowerUpObject;
	int mPowerUpType;
};