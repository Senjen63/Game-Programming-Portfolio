#ifndef EVENTSYSTEM_AVEN_H
#define EVENTSYSTEM_AVEN_H

#include "EventListenerAven.h"
#include "Trackable.h"

#include <list>

class EventSystemAven final : public Trackable
{
    std::list< std::pair< EventType, EventListenerAven * > > mEventListeners{};
public:
    static EventSystemAven & getInstance();
    void addListener( EventType type, EventListenerAven * pEventListener );
    void clearListeners();
    void invokeEvent( const EventAven & event );
    void removeListener( EventType type, EventListenerAven * pEventListener );
};
#endif /* EVENTSYSTEM_AVEN_H */
