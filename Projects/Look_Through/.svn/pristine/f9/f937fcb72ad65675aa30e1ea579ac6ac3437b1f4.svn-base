#pragma once
#include <allegro5/allegro.h>
#include <allegro5/allegro_font.h>
#include <allegro5/allegro_ttf.h>

#include <string>

using namespace std;

class Font //font pointer + size
{
	friend class GraphicsSystem;
public:
	//construct fornt destruct and accessor for size
	Font();
	Font(string, float);
	~Font();

	float getFontSize();
	void setFontSize(float);
	
	void setFont(ALLEGRO_FONT*);

private:
	ALLEGRO_FONT* getFont();
	float mFontSize;
	ALLEGRO_FONT* mpLoadedFont;
};