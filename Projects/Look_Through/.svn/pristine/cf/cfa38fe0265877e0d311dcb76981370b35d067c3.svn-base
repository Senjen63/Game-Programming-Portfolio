#pragma once

#include "HUDEvent.h"

class TimeEvent : public HUDEvent
{
public:
    TimeEvent(float time);
    ~TimeEvent();

    float getTime() const { return mTime; }
private:
    float mTime = 0;
};