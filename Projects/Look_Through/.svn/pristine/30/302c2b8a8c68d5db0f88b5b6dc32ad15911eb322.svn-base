#include "Font.h";

Font::Font(std::string fontLocation, float fontSize)
{
	mFontSize = fontSize;
	pmFont = al_load_ttf_font(fontLocation.c_str(), fontSize, 0);
}

Font::~Font()
{
	al_destroy_font(pmFont);
}