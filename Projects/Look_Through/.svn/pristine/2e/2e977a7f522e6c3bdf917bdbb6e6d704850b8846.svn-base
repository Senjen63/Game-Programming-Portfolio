#pragma once

#include "GameEvent.h"

using namespace std;

enum class GamePlayEventTypes
{
	LOSE = 0,
};

class GameplayEvent : public GameEvent
{
private:
	int mAction = 0;

public:
	GameplayEvent(int keyCode);
	~GameplayEvent();

	int getAction() const { return mAction; }
};