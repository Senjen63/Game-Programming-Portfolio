
#include <PerformanceTracker.h>
#include <MemoryTracker.h>
#include <iostream>
#include <string>
#include <Vector2D.h>
#include <Timer.h>
#include "Game.h"
#undef main
using namespace std;

int main(int argc, char** args)
{
	PerformanceTracker* pPerformTracker = new PerformanceTracker;
	const int mDispWidth = 512;
	const int mDispHeight = 512;
	EventSystem::initInstance();
	Game* game = Game::GetInstance();
	game->Init();
	game->DoLoop();
	delete pPerformTracker;
	delete game;
	EventSystem::cleanupInstance();
	MemoryTracker::getInstance()->reportAllocations(cout);

	system("pause");
	return 0;
}