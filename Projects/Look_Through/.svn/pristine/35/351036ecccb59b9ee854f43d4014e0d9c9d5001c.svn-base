#pragma once
#include <allegro5/allegro.h>
#include "GraphicsBuffer.h"
#include <Vector2D.h>

class Sprite : public Trackable
{
private:
	GraphicsBuffer* mpBitmap;
	Vector2D mLocation;

	int mWidth;
	int mHeight;

public: 
	Sprite(GraphicsBuffer* pBitmap, Vector2D location, int width, int height);

	~Sprite();

	Vector2D getLoc();
	GraphicsBuffer* getBitmap();

	int getWidth();
	int getHeight();

	void setLoc(Vector2D loc);
};