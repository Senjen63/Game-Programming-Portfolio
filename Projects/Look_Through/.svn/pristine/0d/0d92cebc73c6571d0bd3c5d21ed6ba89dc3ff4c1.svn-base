/*
Author: David Silverman
Class: GPR-250-01
Assignment: Assignment 3
Certification of Authenticity:
I certify that this assignment is entirely my own work.
 */

#pragma once
#include <allegro5/allegro.h>
#include <allegro5/allegro_font.h>
#include <allegro5/allegro_ttf.h>
#include <allegro5/allegro_audio.h>
#include <allegro5/allegro_acodec.h>
#include <allegro5/allegro_image.h>
#include <allegro5/allegro_primitives.h>
#include <Trackable.h>
#include <Vector2D.h>
#include <vector>
#include <map>
#include "level.h"
#include "graphicsbuffermanager.h"

using namespace std;

class LevelManager : public Trackable
{
		vector<string> mTitles;
		map<string, Level*> mMap;
	public:
		LevelManager();
		~LevelManager();
		void addLevel(GraphicsBufferManager* manager, string bufferKey, int rows, int colums, int score, float spawn, float speed, int length, int food, string title);
		void addLevel(GraphicsBufferManager* manager, string bufferKey, int rows, int colums, int score, float spawn, float speed, int length, int food, string title, Vector2D xToYScale);
		//void addLevel(GraphicsBufferManager* manager, string bufferKey, int rows, int colums, int score, string title, Vector2D xToYScale, string name);
		Level* getLevel(string levelKey);
		void Clear();
		void Cleanup();
};