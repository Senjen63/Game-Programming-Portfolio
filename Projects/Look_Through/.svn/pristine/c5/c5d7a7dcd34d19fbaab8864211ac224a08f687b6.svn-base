#pragma once

#include <Trackable.h>
#include <Vector2D.h>
#include "GraphicsBuffer.h"

using namespace std;

class Sprite : public Trackable
{
public:
	Sprite(GraphicsBuffer* pBuffer, Vector2D& loc, int width, int height);
	~Sprite();

	GraphicsBuffer* getBuffer();
	Vector2D getLocation();
	int getWidth();
	int getHeight();

private:
	GraphicsBuffer* mpBuffer;
	Vector2D mLoc;
	int mHeight;
	int mWidth;
};