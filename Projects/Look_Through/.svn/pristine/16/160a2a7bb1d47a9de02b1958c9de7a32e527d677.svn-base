#pragma once

#include "GameEvent.h"
#include "Vector2D.h"

class MyMouseEvent : public GameEvent
{
private:
	int mMouseCode = 0;
	Vector2D mMousePos;

public:
	MyMouseEvent(int mouseCode, Vector2D mousePos);
	~MyMouseEvent();

	int getMouseCode() const { return mMouseCode; }
	Vector2D getMousePos() const { return mMousePos; }
};