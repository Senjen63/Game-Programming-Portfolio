#pragma once
#include <cassert>
#include <vector>

#include <MemoryTracker.h>
#include <Vector2D.h>

#include "Unit.h"
#include "Animation.h"

using namespace std;

class Unit;

class UnitManager : public Trackable
{
private:
	vector<Unit*> mpUnits;

public:
	UnitManager();
	~UnitManager();

	Unit* addUnit();
	Unit* addUnit(Animation* pAnim, Vector2D location = DEFAULT_UNIT_POS);

	void deleteUnit(unsigned int index);
	void deleteAll();

	Unit* getUnit(unsigned int index);
	int getIndex(Unit* target);
	unsigned int getLength();

	void updateAll(float dt);
	void drawAll();
};
