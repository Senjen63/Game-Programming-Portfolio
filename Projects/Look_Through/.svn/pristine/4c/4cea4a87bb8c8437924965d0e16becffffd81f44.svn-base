//BP
#pragma once
#include "Unit.h"
#include <Trackable.h>
#include <EventSystem.h>
#include "FoodCreation.h"

class GameFood : public Trackable
{
public:
    GameFood();
    ~GameFood();
    void init();
    void cleaner();
    void updateFoods();
    void levelFinish();
    void setThisFood(Unit* thisFood);
    void setBeenEaten(bool beenEaten);
    bool getBeenEaten() { return mEatFood; }
    Unit* getThisFood() { return mThisFood; }
private:
    bool mEatFood = false;
    Unit* mThisFood = nullptr;
    EventSystem* mpEventSystem = nullptr;
};