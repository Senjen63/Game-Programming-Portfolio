#pragma once

#include <Trackable.h>
#include <vector>
#include "Animation.h"
#include "Unit.h"
#include "GraphicsSystem.h"
#include <Vector2D.h>

const int SMURF_TILE_HALF = 30;

class UnitManager : public Trackable
{
	public:
		UnitManager() {};
		~UnitManager();

		void cleanup();

		void createAndAddUnit(Animation one, Animation two);
		void destroyUnit(Unit* pUnitToDestroy);

		void update(double dt, bool playAnimation);
		void draw(GraphicsSystem* graphSys);

		Unit* getUnit();
		void isPointOnUnit(Vector2D pos);

	private:
		std::vector<Unit*> mpUnits;
};