#pragma once

#include "GraphicsSystem.h"
#include <vector>
#include <Trackable.h>

class Animation : public Trackable
{
public:
    Animation(int fps, int xSize, int ySize, bool loopCondition, GraphicsBuffer* frame);
    ~Animation();
    Sprite getThisSprite();
    Sprite updateSprite(float timePassed);
    void addNewSprite(Sprite sprite);
    void changeSpriteSpeed(int speed);
private:
    int mFps;
    unsigned int mSpriteIndex = 0;
    float mRemainingTime;
    float mSpriteTiming = 1000.0 / mFps;
    bool mKeepLooping = true;
    vector<Sprite> mSpriteList;
};