#include "Component.h"


Unit* Component::getAttachedUnit()
{
	return mpUnit;
}


void Component::setAttachedUnit(Unit* unit)
{
	if (mpUnit != nullptr && unit != mpUnit)
	{
		unit->removeComponent(getIdentifier());
	}

	mpUnit = unit;
}


void Component::removeFromUnit()
{
	mpUnit = nullptr;
}
