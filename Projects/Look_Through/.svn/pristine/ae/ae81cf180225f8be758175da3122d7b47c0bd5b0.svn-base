#pragma once

#include <allegro5/allegro.h>
#include <allegro5/allegro_font.h>
#include <allegro5/allegro_ttf.h>
#include <allegro5/allegro_audio.h>
#include <allegro5/allegro_acodec.h>
#include <allegro5/allegro_image.h>
#include <allegro5/allegro_primitives.h>

#include <iostream>
#include <cassert>
#include <string>

#include "GraphicsBuffer.h"
using namespace std;


class Sprite
{
	friend class GraphicsBuffer;
private: 
	GraphicsBuffer* mBuffer;
	int mSX, mSY, mSWidth, mSHeight;
	

public: 
	Sprite();
	Sprite(GraphicsBuffer* buffer, int x, int y, int width, int height);
	Sprite(GraphicsBuffer* buffer, int x, int y);
	~Sprite();

	int getHeight() { return mSHeight; }
	int getWidth() { return mSWidth; }
	int getX() { return mSX; }
	int getY() { return mSY; }

	GraphicsBuffer* getBuffer() { return mBuffer; }


};