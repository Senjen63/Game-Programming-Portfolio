#pragma once
#include <iostream>
#include <string>
#include <unordered_map>
#include "Trackable.h" 
#include "GraphicsBuffer.h"
class GraphicsBufferManager : public Trackable
{
public:
	GraphicsBufferManager();
	~GraphicsBufferManager();
	void addBuff(string file, char id);
	void removeBuff(char id);
	GraphicsBuffer* getBuff(char id);
	void clearAll();

private:

	unordered_map<char, GraphicsBuffer*> mpBufferList;
	vector<char> mIdList;
	int mTotal;

};
