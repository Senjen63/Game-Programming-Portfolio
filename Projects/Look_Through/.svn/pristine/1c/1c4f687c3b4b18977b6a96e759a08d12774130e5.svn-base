#pragma once

#include "GraphicsBuffer.h"
#include <unordered_map>
#include <string>
#include <Trackable.h>

using namespace std;

typedef string GBKey;

const GBKey MISSING_TEXTURE = "MissingTexture";

const int DEFAULT_SPRITE_WIDTH = 64;
const int DEFAULT_SPRITE_HEIGHT = 64;

class GraphicsBufferManager : public Trackable
{
	public:
		GraphicsBufferManager();
		~GraphicsBufferManager();

		void init();
		void cleanup();

		void addBuffer(GBKey, GraphicsBuffer*);
		void deleteBuffer(GraphicsBuffer*);
		void deleteBuffer(GBKey);

		GraphicsBuffer* getBuffer(GBKey);

		void clearAll();
private:
	unordered_map<GBKey, GraphicsBuffer*> mGraphicsBuffers;

	GraphicsBuffer* mpMissingTexture;
};
