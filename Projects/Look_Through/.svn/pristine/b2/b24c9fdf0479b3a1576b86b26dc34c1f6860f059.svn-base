#pragma once
#include "Unit.h"
#include "UnitTypes.h"
#include "CursorEvent.h"
#include "ActionEvent.h"

#include <Trackable.h>

const int IDLE_INDEX = 0;
const int HOVER_INDEX = 1;

class Button : public Unit
{
    private:
        ButtonTypes mType;

        bool mHovered = false;

        bool mActive = false;

    public:
        Button();
        Button(Vector2D pos, AnimationFrames* idleAnim, AnimationFrames* hoverAnim, int fps, bool loopAnimation, ButtonTypes type);
        ~Button();

        void init() override;

        bool getActive() { return mActive; }
        ButtonTypes getType() { return mType; }

        void setActive(bool active) { mActive = active; }

        void handleEvent(const Event& currentEvent) override;

        void update(float timeElapsed) override;
};