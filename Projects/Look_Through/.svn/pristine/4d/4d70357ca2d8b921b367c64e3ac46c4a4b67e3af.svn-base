#include <iostream>
#include <string>
#include <cassert>
#include <PerformanceTracker.h>
#include <MemoryTracker.h>
#include <Timer.h>
#include <time.h>
#include <stdlib.h>
#include "GraphicsSystem.h"
#include "Game.h"

using namespace std;

int main()
{
	const int DISP_WIDTH = 1920;
	const int DISP_HEIGHT = 1080;
	Game::createGame(Vector2D(DISP_WIDTH, DISP_HEIGHT));
	Game* pSmurfGame = Game::getGame();
	pSmurfGame->init();
	pSmurfGame->loop();
	Game::destroyGame();
	MemoryTracker::getInstance()->reportAllocations(cout);
	system("pause");
	return 0;
}