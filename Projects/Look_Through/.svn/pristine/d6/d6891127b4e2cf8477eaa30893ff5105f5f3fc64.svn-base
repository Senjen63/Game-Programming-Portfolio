#pragma once
#pragma once

#include <Trackable.h>
#include "Animation.h"
#include "GraphicsSystem.h"
#include "Vector2D.h"
#include <vector>

class SnakeUnit : public Trackable
{
private:
	bool mPause;

	//only SnakeUnit Manager can add or delete Units
	SnakeUnit(Animation snake);
	SnakeUnit(Animation snake, Vector2D pos);
	~SnakeUnit();

	Vector2D mLocation;
	int mCurrentAnimation;

	std::vector<Animation> mSnakeLimb;

public:
	friend class SnakeUnitManager;
	
	void draw(GraphicsSystem* graphSys);

	//handles grid movement
	void update(double dt, Vector2D dir);

	void setLocation(Vector2D loc);
	Vector2D getLocation();

};