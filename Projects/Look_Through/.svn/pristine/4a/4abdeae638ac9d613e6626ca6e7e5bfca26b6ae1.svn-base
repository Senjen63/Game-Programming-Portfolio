//     -------------------------------------------------------------------------
//     Final Project
//         Camera.hpp
//     
//             Created:
//                 11-04-2022
//             Author(s):
//                 Aven Presseisen
//     -------------------------------------------------------------------------

#pragma once

#include "BaseComponents.hpp"
#include "GraphicsSystem.hpp"

class Camera
{
    friend class System;

    GraphicsSystem * mpGraphicsSystem;

    Translation mTranslation;
    Scale mScale;
    float mUnitLength;
    
public:
    explicit Camera( GraphicsSystem * pGraphicsSystem );

    Translation & getPosition();
    Scale & getSize();
    
    [[nodiscard]] Rect2DF worldToScreen( const Translation & translation, const Scale & scale ) const;
    [[nodiscard]] Rect2DF uiToScreen( const Translation & translation, const Scale & scale ) const;
    [[nodiscard]] Rect2DF uiToScreenCentered( const Translation & translation, const Scale & scale ) const;
    void setPosition( const Translation & translation );
};
