#pragma once

#include <Trackable.h>
#include <list>
#include "Animation.h"
#include "UnitTypes.h"

const int DEFAULT_X = 0;
const int DEFAULT_Y = 0;
const int DEFAULT_FPS = 60;
const bool DEFAULT_LOOP = true;

class Unit : public Trackable
{
    public:
        Unit();
        Unit(Vector2D, int);
        Unit(Vector2D, GraphicsBuffer*, int, bool, int);
        ~Unit();

        int getAnimCount();
        int getCurrentIndex();
        bool getIsPlaying();
        Vector2D getLocation();
        Vector2D getDimensions();
        bool getDelete();
        int getType();
        int getFps();

        void setDel(bool);
        void setType(int);

        void update(float);
        virtual void updateUnit(float);
        void draw();
        void changeLocation(Vector2D);
        void addAnimation(GraphicsBuffer*);
        void addAnimation(GraphicsBuffer*, int, bool);
        void setAnimation(int);
        void setFps(int);
        void togglePlay(bool);
        bool isMouseOver(Vector2D);
protected:
    Vector2D mLocation;
    vector<Animation> mAnims;
    int mCurrentIndex;
    int mFps;
    bool mPlay = true;
    bool mDelete = false;
    int mType;
};