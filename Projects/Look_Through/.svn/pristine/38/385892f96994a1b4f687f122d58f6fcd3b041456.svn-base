#pragma once

#include <string>
#include <trackable.h>
#include "LevelEvent.h"

using namespace std;

class KeyboardEvent : public LevelEvent
{
public:
	KeyboardEvent(int keyboardInput);
	~KeyboardEvent();

	int getInput() const { return mInput; }

private:
	int mInput;
};