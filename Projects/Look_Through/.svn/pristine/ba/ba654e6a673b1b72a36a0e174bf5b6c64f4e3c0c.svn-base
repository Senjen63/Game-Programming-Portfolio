#pragma once
#include "HUDEvent.h"

class ScoreEvent : public HUDEvent
{
private:
    int mScore = 0;

public:
    ScoreEvent(int score);
    ~ScoreEvent();

    int getScore() const { return mScore; }
};