#pragma once

#include <Vector2D.h>
#include "graphicsBuffer.h"

//No alegro in it
class Sprite
{
	//points too specific graphics buffer with rectangle
	//rectangle (upperlefthand corner + width and height + buffer
	//Contructors to make sprites
public:
	Sprite();
	Sprite(GraphicsBuffer*, Vector2D, int, int);
	~Sprite();

	Vector2D GetSourceLocation();
	void SetSourceLocation(int, int);
	int GetSpriteWidth();
	void SetSpriteWidth(int);
	int GetSpriteHeight();
	void SetSpriteHeight(int);
	GraphicsBuffer* GetGraphicsBuffer();
	void SetGraphicsBuffer(GraphicsBuffer*);

private:
	int mSpriteWidth, mSpriteHeight;
	Vector2D mSourceLocation;
	GraphicsBuffer* mpBitmap;
};