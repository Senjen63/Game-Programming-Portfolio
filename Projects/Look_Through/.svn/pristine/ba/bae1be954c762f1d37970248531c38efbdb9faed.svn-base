#pragma once
#include <Trackable.h>
#include <string>
#include <unordered_map>
#include "Sound.h"

using namespace std;

typedef string SoundKey;

const float MIN_SOUND = 0.0f;
const float MAX_SOUND = 100.0f;

class SoundManager : public Trackable
{
public:
	SoundManager();
	~SoundManager();

	void init();
	void cleanup();

	void addSound(SoundKey key, string soundFile, float volume);

	Sound* getSound(SoundKey key);

	void changeVolume(float newVolume);


private:
	unordered_map<SoundKey, Sound*> mSoundList;

};

