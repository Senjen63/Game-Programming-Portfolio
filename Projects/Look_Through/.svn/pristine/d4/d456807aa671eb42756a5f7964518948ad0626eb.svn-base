#pragma once
#include <PerformanceTracker.h>
#include "GraphicsBuffer.h"

class GraphicsBufferManager : public Trackable
{
public:
	GraphicsBufferManager() {};
	~GraphicsBufferManager() { cleanup(); };

	void cleanup();

	void addGraphicsBuff( string file, string name );
	void deleteGraphicsBuff( string name );

	GraphicsBuffer* getGraphicsBuff( string name );

private:
	unordered_map<string, GraphicsBuffer*> mGBs;
};