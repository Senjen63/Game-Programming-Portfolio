#pragma once

#include "Trackable.h"
#include <vector>
#include "Sprite.h"
#include <Vector2D.h>

class Animation : public Trackable
{
	public:
		Animation();
		~Animation();

		void addSprite(GraphicsBuffer* buffer, int x, int y, int height, int width);
		void singleSprite(GraphicsBuffer* buffer, int x, int y, int height, int width);
		void update(double dt);

		Sprite getCurrentSprite();

	private:
		std::vector<Sprite> mSprites;

		float mTimePerFrame;
		float mTimeLeft;
		int mCurrentSprite;
};