#pragma once
#include "GraphicsBuffer.h"

#include <unordered_map>
#include <string>

#include <Trackable.h>

using namespace std;

typedef string GBKey;

class GraphicsBufferManager : public Trackable
{
	private:
		unordered_map<GBKey, GraphicsBuffer*> mGraphicsBuffers;

	public:
		GraphicsBufferManager();
		~GraphicsBufferManager();

		void cleanup();

		void addBuffer(GBKey key, GraphicsBuffer* newGB);
		void deleteBuffer(GraphicsBuffer* newGB);
		void deleteBuffer(GBKey key);

		GraphicsBuffer* getBuffer(GBKey key);

		void clearAll();
};
