#pragma once

#include "InputSystem.h"
#include "Unit.h"
#include <PerformanceTracker.h>
#include <vector>

using namespace std;


class UnitManager : public Trackable
{
public:
	UnitManager();
	~UnitManager();

	void cleanup();

	void addUnit(Vector2D mousePos, string name);
	//void deleteUnit(int target);
	int getCount();

	void clear();

	void updateAll(float time, int xMove, int yMove, Vector2D foodPos, string playerSprite);
	void updateTime(float time);
	void drawAll(GraphicsSystem* pSystem);

	void pauseUnits();

	void move(int xMove, int yMove);

	Vector2D getHeadPos();
	void setHeadPos(Vector2D);
	Unit* getHeadUnit();
	Unit* getUnit(int target);

	bool checkCollision(int wallArray[10][14]);

private:
	vector<Unit*> mpUnits;
	int mNumUnits;
};