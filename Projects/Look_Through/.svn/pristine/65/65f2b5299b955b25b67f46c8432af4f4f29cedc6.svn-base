#pragma once

#include "Trackable.h"
#include <Vector2D.h>
#include <SDL_events.h>


using namespace std;

class EventSystem : public Trackable
{
		bool mLeft;
		bool mRight;
		bool mUp;
		bool mDown;
		bool mShoot;

		// Event Consts
		const Uint8* mpKey = SDL_GetKeyboardState(NULL);

	public:
		EventSystem();
		~EventSystem();
		void runInput();
		void runGame();

		// setters
		bool getLeft() { return mLeft; }
		bool getRight() { return mRight; }
		bool getUp() { return mUp; }
		bool getDown() { return mDown; }
		bool getShoot() { return mShoot; }
};