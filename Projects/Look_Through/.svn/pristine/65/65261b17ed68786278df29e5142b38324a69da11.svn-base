#pragma once
#include <PerformanceTracker.h>
#include <Timer.h>
#include <Trackable.h>
#include "SnakeUnitManager.h"
#include "SnakeUnit.h"

#include <iostream>
#include <fstream>
using namespace std;

const int SPRITE_SIZE = 32;

const int UP = 1;
const int DOWN = 2;
const int RIGHT = 3;
const int LEFT = 4;

const string LEVEL_1 = "Level1.txt";
const string LEVEL_2 = "Level2.txt";
const string LEVEL_3 = "Level3.txt";
class SnakeManager
{
private:
	//Movement
	Vector2D direction = Vector2D(0,0);

public:
	SnakeManager();
	~SnakeManager();

	void snakeMovementManager(SnakeUnitManager& mSnakeUnitManager, bool up, bool down, bool left, bool right, double targetTime);
	void initialSpawn(SnakeUnitManager& mSnakeUnitManager, Animation& mSnakeAnimation);
};