#pragma once

#include <Trackable.h>
#include <string>

class Sprite;

using namespace std;

class SpriteCreator : public Trackable
{
		int mLoc;

		Sprite* mpShipSprite;
		Sprite* mpShotSprite;

		const string FILE_PATH = "sprite_data.txt";
		const string SHIP_KEY = "ship";
		const string SHOT_KEY = "shot";
		const string SHIP_PATH = "Galahad.bmp";
		const string SHOT_PATH = "Shot.bmp";
	public:
		SpriteCreator();
		~SpriteCreator();

		Sprite* mpShip() { return mpShipSprite; }
		Sprite* mpShot() { return mpShotSprite; }

};