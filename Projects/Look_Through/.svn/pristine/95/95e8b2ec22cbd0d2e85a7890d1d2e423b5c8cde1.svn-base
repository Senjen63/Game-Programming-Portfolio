#pragma once
#include "GameEvent.h"

enum GameEventTypes
{
	LOSE = 1,
	ADD = 2,
	SPEED = 3,
	SLOW = 4,
	WIN = 5,
	ADDPOINTS = 6,
};

class GameplayEvents : public GameEvent
{
private:

	int mEvent = 1;

public:

	GameplayEvents(int event);

	~GameplayEvents();

	int getEvent() const { return mEvent; };
};