#pragma once
#include <PerformanceTracker.h>
#include <MemoryTracker.h>
#include "GraphicsBuffer.h"
#include <unordered_map>

class GraphicsBufferManager : public Trackable
{
public:
    GraphicsBufferManager();
    ~GraphicsBufferManager();

    GraphicsBuffer* makeBuffer(const std::string& index, std::string fileName);
    GraphicsBuffer* getBuffer(const std::string& index);

    void clearBuffer(const std::string& index);
    void clearBuffer(GraphicsBuffer* pBuff);
    void cleanUp();

    friend class Game;
    friend class Unit;
private:
    std::unordered_map<std::string, GraphicsBuffer*> mMap;

};