#pragma once

#include <Trackable.h>
#include <vector>
#include "Sprite.h"

class Animation : public Trackable
{
	private:
		std::vector<Sprite> mSprites;

		float mTimePerFrame;
		float mTimeLeft;
		int mCurrentSprite;

	public:
		Animation();
		~Animation();

		Sprite getCurrentSprite();

		void addAnimatedSprite(GraphicsBuffer* buffer, int x, int y, int height, int width);
		void addSprite(GraphicsBuffer* buffer, int x, int y, int height, int width);

		void update(double dt);
		void speedUp();
		void slowDown();
};