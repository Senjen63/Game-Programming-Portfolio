#pragma once

#include "Trackable.h"
#include <string>

class Vector2D;

using namespace std;

const string FILE_PATH = "input_system.txt";

class InputSystem : public Trackable
{
	private:
		int mNullVal;
		
	public:
		InputSystem();
		~InputSystem();

		enum MouseButtons
		{
			LEFT_MOUSE = 1,
			RIGHT_MOUSE = 2,
			MIDDLE_MOUSE = 4
		};

		enum KeyBoardEventKeys
		{
			ESCAPE_KEY = 27,
			ENTER_KEY = 13,
			LEFT_KEY = 1073741904,
			RIGHT_KEY = 1073741903,
			UP_KEY = 1073741906,
			DOWN_KEY = 1073741905,
			SPACE_KEY = 32,
			QUIT = 256
		};

		void init();
		void cleanUp();

		void update();

		// getters 
		//Vector2D getMousePosition();
		//bool getKeyState();
		//bool getMouseState(MouseButtons);
};