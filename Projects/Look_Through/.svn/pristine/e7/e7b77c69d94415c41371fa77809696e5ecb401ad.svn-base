#pragma once

#include <SDL.h>
#include <SDL_image.h>
#include <Trackable.h>
#include <iostream>

class GraphicsBuffer : public Trackable
{
public:
	friend class GraphicsBufferManager;
	friend class GraphicsSystem;

	GraphicsBuffer(int width, int height);
	GraphicsBuffer(std::string file);
	~GraphicsBuffer();

	int getWidth() { return mpSurface->w; }
	int getHeight() { return mpSurface->h; }

private:
	bool mOwnsSurface;

	SDL_Surface* mpSurface;
	SDL_Surface* getSurface() { return mpSurface; }
};