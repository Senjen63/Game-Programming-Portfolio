#pragma once
#include "GameEvent.h"

class MessageEvent : public GameEvent
{

    private:
        string mText;

    public:
        MessageEvent(string text);
        ~MessageEvent();

        string getMessage() const { return mText; }

};
