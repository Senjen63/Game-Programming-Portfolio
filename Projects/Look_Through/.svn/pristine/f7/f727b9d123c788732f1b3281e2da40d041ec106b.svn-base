#pragma once

#include "Trackable.h"
#include "GraphicsBuffer.h"


class Sprite
{
public:
	Sprite(GraphicsBuffer* imgBuffer, Vector2D pos, Vector2D size);
	~Sprite();
	GraphicsBuffer getBuffer();
	Vector2D getPos();
	Vector2D getSize();
	GraphicsBuffer getSpriteGraphicsBuffer();

private:
	GraphicsBuffer* mImgBuffer;
	Vector2D mPos;
	Vector2D mSize;
};

