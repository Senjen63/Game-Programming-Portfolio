/*
Author: David Silverman
Class: GPR-250-01
Assignment: Assignment 3
Certification of Authenticity:
I certify that this assignment is entirely my own work.
 */

#pragma once
#include "font.h"
#include "color.h"
#include "sprite.h"
#include "graphicsbuffer.h"
#include <string>
#include <Trackable.h>

using namespace std;

class GraphicsSystem : public Trackable
{
		ALLEGRO_DISPLAY* mpDisp;
		ALLEGRO_BITMAP* mpBack;
		const int WINDOW_WIDTH = 800;
		const int WINDOW_HEIGHT = 600;
	public:
		GraphicsSystem();
		~GraphicsSystem();
		void Init();
		void Init(int width, int height);
		void Cleanup();
		int getHeight();
		int getWidth();
		void setHeight(int hi);
		void setWidth(int wi);
		void Flip();
		void Draw(Vector2D loc, Sprite spri);
		void Draw(Vector2D loc, Sprite spri, float scale, float scaleY);
		void Draw(GraphicsBuffer* gBuff, Vector2D loc, Sprite spri);
		void Draw(GraphicsBuffer* gBuff, Vector2D loc, Sprite spri, float scaleX, float scaleY);
		void WriteText(Vector2D loc, Font font, Color color, string text);
		void WriteText(GraphicsBuffer* gBuff, Vector2D loc, Font font, Color color, string text);
		void SaveBuffer(string fileName);
		void SaveBuffer(GraphicsBuffer* gBuff, string fileName);
};