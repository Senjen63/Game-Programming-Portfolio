#pragma once
#include "Unit.h"
#include <unordered_map>
#include <vector>
#include <Trackable.h>

class UnitManager : public Trackable
{
private:
	vector<Unit> mUnits;

public:
	UnitManager();

	~UnitManager();

	void setAnimation();

	void addUnit(Unit unit);

	void removeUnit(Vector2D location);

	Unit getUnit(Vector2D location);

	Unit getRecentUnit();

	void removeAll();

	void updateAll(double dt);

	void drawAll(int scale);

	bool isEmpty();
};