#pragma once

#include <iostream>
#include <Trackable.h>
#include <SDL_mixer.h>
#include <SDL.h>
#include <SDL_audio.h>
#include <vector>

using namespace std;

class SFX : public Trackable
{
private:
	vector<Mix_Chunk*> mSoundEffectBank;

public:
	SFX();
	~SFX();

	void addSoundEffect(const char* path);

	void initializeSFX(const char* eat, const char* babb, const char* friendSound, const char* tobb, const char* treeThunk);

	void playSoundEffect(const int choice) const;

};