#pragma once

#include "Trackable.h"
#include <SDL.h>
#include "Color.h"
#include "Sprite.h"
#include "Font.h"

class HUD : public Trackable
{
		const string FILE_PATH = "hud_run_data.txt";
		const string LIVES = "Lives:";
		const string SCORE = "Score: ";

		Vector2D mLife1;
		Vector2D mLife2;
		Vector2D mLife3;
		Vector2D mLives;
		Vector2D mScore;

		int mWhole;
		int mHalf;
		int mMost;
		int mMiddle;
		int mLeast;

		float mHeight;

		Color* mpColor;
		Font* mpFont;
		Sprite* mpLife;
	public:
		HUD();
		void setLifePos(Vector2D vec1, Vector2D vec2, Vector2D vec3);
		void setLivesPos(Vector2D);
		void setScorePos(Vector2D);
		void setColor(Color*);
		void setFont(Font*);
		void setSprite(Sprite*);
		void drawHUD(int lives, int score);
};
