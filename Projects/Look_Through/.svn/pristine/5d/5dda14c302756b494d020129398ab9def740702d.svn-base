#include "Font.h"

using namespace std;

Font::Font( string fileName, int fontSize )
{
	mFontSize = fontSize;
	mpFont = al_load_ttf_font( ( ASSET_PATH + fileName ).c_str(), mFontSize, 0 );
}

Font::~Font()
{
	al_destroy_font(mpFont);
}