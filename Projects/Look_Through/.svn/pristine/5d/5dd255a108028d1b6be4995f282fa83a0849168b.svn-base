#pragma once

#include <allegro5/allegro_image.h>
#include <allegro5/allegro_primitives.h>
#include "Trackable.h" 
#include "Color.h"
#include <Vector2D.h>
#include <string>

using namespace std;


class GraphicsBuffer : public Trackable
{
	
	friend class GraphicsSystem;

public:

	GraphicsBuffer(string file);
	GraphicsBuffer(GraphicsBuffer* buff);
	GraphicsBuffer(Color color, int height, int width);
	~GraphicsBuffer();
	int getWidth();
	int getHeight();

private:

	bool mWillDestroy;
	ALLEGRO_BITMAP* mBitMap;
	GraphicsBuffer(ALLEGRO_BITMAP* buff);

};
