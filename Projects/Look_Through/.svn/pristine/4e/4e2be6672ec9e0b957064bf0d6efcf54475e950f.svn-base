#pragma once

#include "GameEvent.h"

enum HudEventType
{
	SCORE_EVENT = NUM_GAME_EVENT_TYPES,
	TIME_EVENT,
	FPS_EVENT,
	START_EVENT,
	LOSE_EVENT,
	NUM_HUD_EVENT_TYPES
};

class HudEvent : public GameEvent
{
private:
	

public:
	HudEvent(HudEventType event);
	~HudEvent();
};