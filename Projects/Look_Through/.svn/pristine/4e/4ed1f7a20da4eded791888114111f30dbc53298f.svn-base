#include "Font.h"
Font::Font(std::string filePath, int size)
{
	mpFont = al_load_ttf_font((filePath).c_str(), size, 0);
}
Font::~Font()
{
	al_destroy_font(mpFont);
}

ALLEGRO_FONT* Font::getFont()
{
	return mpFont;
}
int Font::getFontSize()
{
	return fontSize;
}