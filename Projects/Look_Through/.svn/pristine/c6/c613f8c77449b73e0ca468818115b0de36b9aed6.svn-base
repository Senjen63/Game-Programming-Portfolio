#pragma once

#include "Event.h"

enum GameEventType
{
	INPUT_EVENT,
	GAME_MOUSE_EVENT,
	ENEMY_DEATH_EVENT
};

class GameEvent : public Event
{
public:
	GameEvent(GameEventType gameEvent);
	virtual ~GameEvent();
private:
};
