//BP
#pragma once
#include <Vector2D.h>
#include "GameEvents.h"

class DirectionChange : public GameEvents
{
public:
    DirectionChange(Vector2D path);
    ~DirectionChange();
    Vector2D getPath() const { return mPath; }
private:
    Vector2D mPath = Vector2D(0, 0);
};