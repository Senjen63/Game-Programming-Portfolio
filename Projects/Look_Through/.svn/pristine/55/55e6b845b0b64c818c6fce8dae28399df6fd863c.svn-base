//BP
#pragma once
#include "GameEvents.h"

class GameMessages : public GameEvents
{
public:
    GameMessages(string messageText);
    ~GameMessages();
    string getGameMessage() const { return mMessage; }
private:
    string mMessage;
};