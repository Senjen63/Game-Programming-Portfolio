	#pragma once

#include "Animation.h"
#include "GraphicsSystem.h"
#include "GraphicsBufferManager.h"

#include <Vector2D.h>
#include <string>

const int SPRITE_HEIGHT = 60;
const int SPRITE_WIDTH = 60;
const int WALL_HEIGHT = 50;
const int WALL_WIDTH = 50;

class Unit : public Trackable
{
public:
	Unit();
	Unit(Animation* pCurrent, Animation* pAlt, Vector2D location);
	Unit(string currentAnimKey, string altAnimKey, Vector2D pos);
	Unit(Vector2D loc, GraphicsBuffer* pBuffer);
	Unit(string wallKey, Vector2D pos);
	Unit(string foodKey, Vector2D pos, int width, int height);
	~Unit();

	float getTime();

	void Update(double dt);
	void Draw(GraphicsSystem* display);
	void swapAnim();
	void changeTiming(float timerChange);
	void pauseUnpause();
	Vector2D getPos();
	void setPos(Vector2D pos);

private:
	Animation* mpCurrentAnim = nullptr;
	Animation* mpAltAnim = nullptr;
	Vector2D mLocation;
	bool paused = false;
};
