#pragma once

#include <PerformanceTracker.h>
#include <vector>
#include <iostream>
#include "Vector2D.h"
#include <vector>
#include "Animation.h"
#include "Unit.h"

using namespace std;
class Unit;

class UnitManager : public Trackable
{
public:
	UnitManager();
	~UnitManager();
	void cleanup();
	void addUnit(Vector2D location, Animation* pAnimation1, Animation* pAnimation2);
	void deleteUnit(Vector2D location);
	Unit* getLastUnit();
	void updateAll(float time);
	void drawAll(GraphicsSystem* pGraphicsSystem);
	int getUnitCount();
	void setPause();
	friend class Game;
private:
	vector<Unit*> mpUnits;
	int mUnitCount;
};