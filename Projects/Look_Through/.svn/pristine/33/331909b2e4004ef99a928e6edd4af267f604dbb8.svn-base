#pragma once

#include "Trackable.h"
#include "Vector2D.h"
#include "GraphicsBuffer.h"

class Sprite : public Trackable
{
friend class GraphicsSystem;

private:

	GraphicsBuffer* mpBuffer;
	Vector2D mLocation;
	int mWidth;
	int mHeight;

public:

	//Sprite(Vector2D* pLocation, GraphicsBuffer* pBuffer);
	Sprite(Vector2D pLocation, GraphicsBuffer* pBuffer, int newWidth, int newHeight);
	~Sprite() {};

	int getSpriteWidth() { return mWidth; }
	int getSpriteHeight() { return mHeight; }
	GraphicsBuffer* getSpriteBuffer() { return mpBuffer; }
	Vector2D getSpriteSourceLocation() { return mLocation; }
};