#pragma once

#include "Sprite.h"
#include <vector>

const int CHARACTER = 64;
const int TREE = 256;

class AnimationPrototype : public Trackable
{
public:
	friend class AnimationManager;
	friend class Animation;

	AnimationPrototype(GraphicsBuffer* buffer, int type);
	~AnimationPrototype();

	void addSprite(const Sprite& theSprite);

	const Sprite& getSprite(int index) const;
	unsigned int getNumSprites() const { return mSprites.size(); }

private:
	std::vector<Sprite> mSprites;
};