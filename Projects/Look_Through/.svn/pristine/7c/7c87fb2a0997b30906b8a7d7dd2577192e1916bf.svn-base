#pragma once

#include <vector>

#include <PerformanceTracker.h>
#include <MemoryTracker.h>

#include "Unit.h"

using namespace std;

class UnitManager : public Trackable
{
public:
	UnitManager();
	~UnitManager();

	void addUnit(Unit* unit);
	void addUnit(Sprite sprite, Square* square);
	void clearList();
	void deleteUnits(Vector2D deleteLocation, int unitSize);
	void killUnit(int num);
	void drawAll(GraphicsSystem* system);
	void updateAll(float timePassed);
	int getAmount();

	int getPoolAmount();
	
	Unit* getUnit(int num);

private:
	vector<Unit*> mUnitList;
	vector<Unit*> mUnitPool;
};
