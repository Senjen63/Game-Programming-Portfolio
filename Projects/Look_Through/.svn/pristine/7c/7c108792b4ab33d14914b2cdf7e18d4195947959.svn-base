#pragma once
#include "GameEvents.h"


enum GameplayEventTypes
{
	EVENT_GAME_CLOSE = 1,
	EVENT_GAME_WIN = 2,
	EVENT_GAME_START = 3,
};

class GameplayEvents : public GameEvents
{
private:

	int mEvent = 0;

public:

	GameplayEvents(int event);

	~GameplayEvents();

	int getEvent() const { return mEvent; };
};
