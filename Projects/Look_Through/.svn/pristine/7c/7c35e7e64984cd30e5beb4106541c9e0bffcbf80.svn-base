#pragma once

#include <string>

#include <Trackable.h>

struct ALLEGRO_BITMAP;

using namespace std;

class GraphicsBuffer : public Trackable
{
friend class GraphicsSystem;public:

	GraphicsBuffer();
	GraphicsBuffer(int width, int height);
	GraphicsBuffer(string fileName);
	~GraphicsBuffer();

	int getHeight();
	int getWidth();

private:

	GraphicsBuffer(ALLEGRO_BITMAP* pBitmap);

	ALLEGRO_BITMAP* mpBitmap = nullptr;

	bool mOwnsBitmap = true;

	const int mDEFAULT_HEIGHT = 100;
	const int mDEFAULT_WIDTH = 100;

	//friend class GraphicsSystem;
};

