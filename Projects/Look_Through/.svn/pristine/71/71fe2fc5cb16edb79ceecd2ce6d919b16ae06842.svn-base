#pragma once
#include <Trackable.h>
#include <vector>
#include "InputSystem.h"

//hold a list of Sprites and timing information to switch between them
class Animation : public Trackable
{
private:

	bool mAniLoop;//start at beginning of animation after reaching end
	//Sprite* mpSpriteList;//make list of sprites
	vector<Sprite> mpSprites;
	int mCurrentSprite;
	int mSpriteCount;
	int mCount;
	int mTimePerFrame = 1000.0/15;
	float mTime;//fps
	float mTimeLeft;
	
public:

	Animation();
	Animation(GraphicsBuffer* pAniBuffer, float time, int spriteCount, bool aniLoop);
	~Animation();

	void speedUpSlowDown(float input); //adjust time to make animation faster or slower
	void update(double dt); //find current sprite

	Sprite getCurrentSprite();
	float getTime();
};