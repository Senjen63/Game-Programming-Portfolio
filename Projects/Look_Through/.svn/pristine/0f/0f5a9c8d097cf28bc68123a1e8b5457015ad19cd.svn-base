#pragma once
#include <PerformanceTracker.h>
#include <MemoryTracker.h>
#include <array>
#include "UnitManager.h"
#include "GraphicsBuffer.h"
#include "HUD.h"
using namespace std;

class Level : public Trackable
{
public:
	friend class Game;

	Level();
	Level(string message);
	Level(int wallArray[10][14], string path, string sprite);
	~Level();

	void cleanup();

	void draw(ALLEGRO_DISPLAY* backBuff, GraphicsBuffer* pBuff, int scale);
	void drawHUD(Font* font);
	void drawHUD(Font* pFont, int score, float FPS, float time);

private:
	UnitManager mWallMan;
	HUD* mHUD;
	int wallArray1[10][14];
	string mSprite;
	Font* mFont;
};