/*
A pointer to a specific part of a GraphicsBuffer

Data
• Pointer to the GraphicsBuffer which contains the bitmap
• Source loc (upper left-hand corner of where sprite begins)
• Width and height of sprite

Functions
• Constructor(s)/destructor
• Accessors( source loc, height, width, GraphicsBuffer )
*/

#include "Sprite.h"

Sprite::Sprite(GraphicsBuffer* pBitmap, Vector2D sourceLoc, Vector2D widthHeight)
{
	mpGraphicsBuffer = pBitmap;
	mSourceLoc = sourceLoc;
	mWidthHeight = widthHeight;
}

Sprite::~Sprite()
{
	//delete mpGraphicsBuffer;
	//mpGraphicsBuffer = nullptr;
}

GraphicsBuffer* Sprite::getGraphicsBufferSprite() 
{
	return mpGraphicsBuffer;
}

Vector2D Sprite::getWidthHeight()
{
	return mWidthHeight;
}

Vector2D Sprite::getSourceLoc() 
{
	return mSourceLoc;
}