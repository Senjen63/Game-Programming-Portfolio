#include <iostream>
#include <cassert>
#include <string>
#include <map>

#include <PerformanceTracker.h>
#include <MemoryTracker.h>

#include "GraphicsBufferManager.h"

//----------------------------------------Graphics Buffer Manager----------------------------------------
GraphicsBufferManager::GraphicsBufferManager()
{
	Clear();
}

GraphicsBufferManager::~GraphicsBufferManager()
{
	Clear();
	std::map<std::string, GraphicsBuffer*>().swap(grahpicsBufferMap); //this swap makes unitMap empty, and temp unitMap is destroyed
}