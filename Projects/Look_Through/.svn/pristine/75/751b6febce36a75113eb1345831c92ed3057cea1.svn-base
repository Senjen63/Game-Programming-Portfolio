#pragma once

#include <EventListener.h>
#include <Vector2D.h>

#include "Color.h"

class Font;

class HUD : public EventListener
{
public:

	HUD(Font* pFont, Color& baseCol);
	~HUD();

	void draw();
	void handleEvent(const Event& theEvent);
	void resetTime();
	void resetScore();

private:

	Font* mpFont = nullptr;
	
	Color mBaseCol;

	int mScore = 0;	
	float mTime = 0;
	int mFPS = 0;

	Vector2D mScorePos = Vector2D(5,5);
	Vector2D mTimePos = Vector2D(5, 25);
	Vector2D mFPSPos = Vector2D(5, 45);

	string mScoreText = "Score:   ";
	string mTimeText = "Level Time: ";
	string mFPSText = "FPS:      ";
};

