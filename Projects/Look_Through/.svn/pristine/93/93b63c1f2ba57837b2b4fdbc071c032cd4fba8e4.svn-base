#pragma once
#include "Animation.h"
#include "Vector2D.h"
#include <PerformanceTracker.h>
#include <MemoryTracker.h>
#include "Unit.h"
#include "UnitManager.h"
class Obstacle : public Unit
{
private:
	float mSpeed;
	int mMinDistance;
	Vector2D mStartPos;
	int mMaxRand;
	int mMinRand;
public: 
	using Unit::Unit;
	void Update(float t) override;
	void SetSpeed(float speed);
	void SetMaxDistance(int dist);
	void SetInstanceVars(Vector2D startPosition, int maxRandom, int minRandom);
};