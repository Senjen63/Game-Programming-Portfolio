#pragma once

#include <Vector2D.h>
#include "GameEvent.h"

class MoveEvent : public GameEvent
{
public:
	MoveEvent(const int &pos);
	~MoveEvent();
	int getKey() const { return mKey; };

private:
	int mKey;
};