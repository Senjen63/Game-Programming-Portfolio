#pragma once

#include <allegro5/allegro.h>
//could just have rgb nums and convert to alegro colors when needed may or may not have alegre
class Color
{
public:
	Color();
	Color(int, int, int, int);
	~Color();

	int getR();
	int getG();
	int getB();
	int getA();
	void setR(int);
	void setG(int);
	void setB(int);
	void setA(int);

	void setColor();
	ALLEGRO_COLOR getColor();//move to private add graphicsSystem as friend

private:
	ALLEGRO_COLOR mColor;
	int mR, mG, mB, mA;
};