#pragma once
#include "Font.h"
#include "EventSystems/EventSystemAven.h"

class HUD final : public Trackable
{
    int mWidth = 800, mHeight = 600;
    Font * mpTitleFont;
    Font * mpMenuOptionsFont;
    Font * mpInGameFont;

public:
    HUD();
    ~HUD();


    void drawEnd( bool won, int gameScore ) const;
    void drawGame( int fps, int score ) const;
    void drawMenu( int option ) const;
    void loadHUD( int width, int height, Font & titleFont, Font & menuOptionsFont, Font & inGameFont );
};
