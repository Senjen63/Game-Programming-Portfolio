#pragma once

#include "AnimationPrototype.h"

class Animation : public Trackable
{
public:
	Animation(AnimationPrototype* pProto, float timePerFrame, bool looping = true);
	~Animation() {};

	void setTimePerFrame(float newTimePerFrame);
	float getTimePerFrame() const { return mTimePerFrame; }

	const Sprite& getCurrentSprite() const;
	void update(double dt);

private:
	AnimationPrototype* mpPrototype;
	int mCurrentSprite;
	bool mLooping;
	float mTimePerFrame;
	float mTimeUntilNextFrame;
};