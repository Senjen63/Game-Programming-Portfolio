#pragma once
#include <Vector2D.h>

#include "SpawnEvent.h"
#include "GraphicsBuffer.h"

class SpawnSpikeEvent : public SpawnEvent
{
private:
    Vector2D mPos;
    GraphicsBuffer* mpBuffer = nullptr;

public:
    SpawnSpikeEvent(Vector2D pos, GraphicsBuffer* buffer);
    ~SpawnSpikeEvent();

    Vector2D getPos() const { return mPos; }
    GraphicsBuffer* getBuffer() const { return mpBuffer; }
};