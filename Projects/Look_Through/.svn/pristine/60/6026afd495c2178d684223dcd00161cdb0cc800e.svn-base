#pragma once

#include <Vector2D.h>
#include "Animation.h"
#include "GraphicsSystem.h"

class Unit : public Trackable
{
	friend class Game;
public:
	Unit();
	Unit(Vector2D location, Animation * animationPrime, Animation * animationSecondary);
	~Unit();

	void update();
	void draw(GraphicsSystem* system);
	void setAnimation();
	void setLocation(Vector2D position);

private:
	Vector2D mLocation;
	Animation * mpPrimaryAnimation;
	Animation * mpAlternateAnimation;
	Animation * mpCurrentAnimation;
	const float DEFAULT_SCALE = 1;
};

