#pragma once

#include <PerformanceTracker.h>
#include "UnitManager.h"

struct Level
{
	UnitManager mLevel;
	int mLength = 0, mSpeedSpawn = 0, mSlowSpawn = 0, mScoreSpawn = 0, mScorePoints = 0, mStartingLength = 0;
};

class LevelManager : public Trackable
{
private:
	int mNumLevels = 0;
	Level* mpLevels = nullptr;

public:
	LevelManager();
	~LevelManager();

	void init(int numLevels);
	void cleanUp();
	void createSideWalls(int level);
	void createInnerWall(int level, Vector2D location);
	void loadLevelData(int level, int length, int speedSpawn, int slowSpawn, int scoreSpawn, int scorePoints, int startingLength);
	bool detectUnits(int level, Vector2D location);
	void draw(GraphicsSystem* pGraphicsSystem, int index);

	int getNumLevels();
	int getLevelLength(int level);
	int getSpeedSpawn(int level);
	int getSlowSpawn(int level);
	int getScoreSpawn(int level);
	int getScorePoints(int level);
	int getStartingLength(int level);
};