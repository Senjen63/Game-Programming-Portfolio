#pragma once

#include <PerformanceTracker.h>
#include <unordered_map>
using namespace std;

class GraphicsBuffer;

class GraphicsBufferManager : public Trackable
{
public:
	GraphicsBufferManager();
	~GraphicsBufferManager();
	GraphicsBuffer* addBuffer(string key, string fileName);
	void deleteBuffer(string key);
	void deleteBuffer(GraphicsBuffer* pBuffer);
	GraphicsBuffer* getBuffer(string key);
	void cleanup();
	friend class Game;
private:
	unordered_map<string, GraphicsBuffer*> mMap;
};