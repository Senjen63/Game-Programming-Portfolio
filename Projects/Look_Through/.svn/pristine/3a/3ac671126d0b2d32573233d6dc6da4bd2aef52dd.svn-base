#pragma once

#include "SDLInclude.h"
#include <Trackable.h>
#include <vector>

#include <iostream>

class Sound : public Trackable
{
private:
	std::vector<Mix_Chunk*> mpSounds;
	bool mCanPlay = true;
public:
	Sound();
	~Sound();

	void addSound(const char* path);
	void playSound(const int sound) const;
	void playSound(const int sound, int loop) const;// loop -1 for infinite
	void stopSound();

	bool getCanPlay()				{ return mCanPlay;		};
	void changeCanPlay(bool change) { mCanPlay = change;	};
};