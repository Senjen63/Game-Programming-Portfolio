//BP
#pragma once
#include <Vector2D.h>
#include "GameEvents.h"

class EndGameLevel : public GameEvents
{
public:
    EndGameLevel(bool next);
    ~EndGameLevel();
    bool getNext() const { return mProceed; }
private:
    bool mProceed = false;
};