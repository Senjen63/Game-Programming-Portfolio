#pragma once
#include <string>
#include <fstream>

using namespace std;

class SaveData
{
public:
	SaveData();
	~SaveData();

	void SaveTheData(int, int);
	void LoadData();

	int GetPlayerOneScore();
	int GetPlayerTwoScore();

private:
	int mPlayerOneScore = 0, mPlayerTwoScore = 0;
	const string fileName = "saveInfo.txt";
};
