//BP
#pragma once
#include "GameEvents.h"

class GameActions : public GameEvents
{
public:
    GameActions(int tag);
    ~GameActions();
    int getAction() const { return mGameActionTag; }
private:
    int mGameActionTag = 0;
};