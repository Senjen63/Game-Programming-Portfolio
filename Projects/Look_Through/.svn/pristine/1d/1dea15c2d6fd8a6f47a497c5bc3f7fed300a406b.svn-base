#pragma once

#include "GraphicsBuffer.h"
#include <Vector2D.h>
#include <Trackable.h>
using namespace std;


class Sprite : public Trackable
{
private:

	GraphicsBuffer* mpBuffer;
	Vector2D mSourceLocation;
	int mWidth;
	int mHeight;

public:

	Sprite();
	Sprite(GraphicsBuffer*, int, int, Vector2D);
	~Sprite();

	Vector2D getSourceLoc();

	int getWidth();
	int getHeight();

	GraphicsBuffer* getGraphicsBuffer();
};