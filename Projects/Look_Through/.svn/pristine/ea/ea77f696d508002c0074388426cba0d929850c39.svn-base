#pragma once
#include "Unit.h"

class HUDText : public Unit
{
public:
	HUDText(std::pair<float, float>location, std::pair<float, float>scale, std::string text, std::string animationDataFilePath);
	virtual ~HUDText();

	virtual void draw(GraphicsSystem* currentGraphicsSystem);
	void updateText(std::string newText) { mDisplayText = newText; }

private:
	std::string mDisplayText;
	GraphicsBuffer* mpTextBuffer = nullptr;
	Font* mpFont;
};