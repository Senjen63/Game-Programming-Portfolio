#pragma once

#include "Trackable.h"
#include "Unit.h"

#include <vector>

class UnitManager : public Trackable
{
public:
	UnitManager();
	~UnitManager();
	Unit* addNewUnit(int fps, Vector2D location);
	void deleteUnit(Unit* selctedUnit);
	void deleteUnit(int UnitID);
	void updateAll(double doub);
	void drawAll(GraphicsSystem* gameGraphics);
	void cleaner();
	int getUnitCount();
	Unit* getUnitAtEnd();
	Unit* getSpecificUnit(Unit* selectedUnit);
	Unit* getSpecificUnit(Vector2D mouseLoc);
private:
	vector<Unit*> mpUnitList;
};