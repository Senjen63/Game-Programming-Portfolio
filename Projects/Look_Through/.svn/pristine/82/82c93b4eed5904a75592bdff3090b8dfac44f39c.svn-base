#pragma once
#include <queue>
#include "GraphicsBuffer.h"
#include <unordered_map>
#include <vector>

class GraphicsBufferManager : public Trackable
{
private:
		unordered_map<string, GraphicsBuffer*> mpGraphicsBuffers;

public:

	GraphicsBufferManager();

	~GraphicsBufferManager();

	void addGraphicsBuffer(string key, GraphicsBuffer* buffer);

	GraphicsBuffer* &getGraphicsBuffer(string key);

	void removeGraphicsBuffer(string key);

	void deleteAll();
};
