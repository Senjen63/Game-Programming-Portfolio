#include "SnakeEvents.h"

ChangeDirectionEvent::ChangeDirectionEvent(SnakeEventTypes type, Direction dir) : Event( ( EventType )type )
{
	mDir = dir;
}

CollisionEvent::CollisionEvent( SnakeEventTypes type, Unit* collider ) : Event( ( EventType )type )
{
	mpCollider = collider;
}

ChangeSpeedEvent::ChangeSpeedEvent( SnakeEventTypes type, int speed ) : Event( ( EventType )type )
{
	mChange = speed;
}

AddPointsEvent::AddPointsEvent( SnakeEventTypes type, int points ) : Event( ( EventType )type )
{
	mPoints = points;
}
