#pragma once

#include <Event.h>

enum class GameEventType
{
	KEYBOARD_JUST_PRESS_EVENT = NUM_BASE_EVENT_TYPES,
	MOUSE_MOVE_EVENT,
	SCORE_CHANGE_EVENT,
	FRUIT_COLLECTED_EVENT,
	SNAKE_DIED,
	SNAKE_WON,
	NUM_GAME_EVENT_TYPES
};

class GameEvent : public Event
{

public:

	GameEvent(GameEventType type) : Event((EventType) type) {}

	~GameEvent() {}
};

