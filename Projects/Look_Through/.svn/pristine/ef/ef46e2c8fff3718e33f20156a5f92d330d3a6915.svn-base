#pragma once

#include"animation.h"
#include<Trackable.h>

#include<iostream>
#include<vector2D.h>
#include<list>

class Unit : public Trackable
{
public:
	Unit();
	Unit(Vector2D position, int fps);
	~Unit();

	Animation& currentAnimation();

	void addAnimation(Animation);

	void unitUpdate(float time);

	void setAnimation(int);

	void setFps(int fps);

	void changePosition(Vector2D newPos);;
	
	void draw(Vector2D, Vector2D, Vector2D, Vector2D, int);

	int getAnimationVectorCount();
	int getCurrentIndex();

	Vector2D getLocation();

private:
	Vector2D mLocation;
	vector<Animation> mAnimations;
	int mCurrentIndex;
	int mFps;
};
