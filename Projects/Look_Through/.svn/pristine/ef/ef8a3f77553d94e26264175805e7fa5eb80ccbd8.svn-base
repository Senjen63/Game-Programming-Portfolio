#pragma once

#include "Event.h"

enum GameEventType
{
	INPUT_EVENT,
	MOVE_EVENT,
	POWER_UP_EVENT,
	SNAKE_COLLISION_EVENT,
	SCORE_EVENT,
	WIN_EVENT
};

class GameEvent : public Event
{
public:
	GameEvent(GameEventType gameEvent);
	virtual ~GameEvent();
private:
};