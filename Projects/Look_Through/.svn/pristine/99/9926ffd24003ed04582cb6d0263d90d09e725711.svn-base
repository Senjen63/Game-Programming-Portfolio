#pragma once
#include <string>
#include <unordered_map>

#include <MemoryTracker.h>

#include "GraphicsBuffer.h"

using namespace std;

class GraphicsBufferManager : public Trackable
{
private:
	unordered_map<string, GraphicsBuffer*> mpBuffers;

public:
	GraphicsBufferManager();
	~GraphicsBufferManager();

	GraphicsBuffer* getBackBuffer();

	void deleteBuffer(string key);
	void deleteAll();

	GraphicsBuffer*& operator [](string key);
};
