#pragma once
#include <MemoryTracker.h>
#include <PerformanceTracker.h>
#include <vector>

#include "Sprite.h"
class Animation : public Trackable
{
private:
	float mTime;
	float mCurrentTime;
	int mCurrentSprite;
	int mNumSprites;
	bool mLoop;
	vector<Sprite*> mpSpritesheet;
	int currIncrement = 0;

public:
	Animation();
	Animation(GraphicsBuffer* pBuffer, int numSprites, float timing, bool loop, int columns, int rows);
	~Animation();

	void Update();

	Sprite* GetCurrentSprite();

	void AdjustTime(float modifier);
	float GetTime();
};