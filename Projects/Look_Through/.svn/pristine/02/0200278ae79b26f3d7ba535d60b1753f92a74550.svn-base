#pragma once

#include <string>
#include <Trackable.h>
#include <Event.h>

enum HudEventTypes
{
	SCORE_EVENT = NUM_BASE_EVENT_TYPES,
	TEXT_EVENT,
	FPS_EVENT,
	TIME_EVENT,
	NUM_HUD_EVENT_TYPES
};

class HudEvent : public Event
{
public:
	HudEvent(HudEventTypes type);
	virtual ~HudEvent();
private:

};