/*
A class to hold a chunk of memory used to display something

Data
• An Allegro bitmap

Functions
• Constructor(s)/destructor
• Accessors( height, width )
*/

#pragma once
#include <allegro5/allegro_image.h>
#include <allegro5/allegro_primitives.h>

#include "Color.h"

#include <Trackable.h>
#include <string>
#include <Vector2D.h>

using std::string;


class GraphicsBuffer : public Trackable
{
	friend class GraphicsSystem;

private:
	bool mOwnsBuffer;

	ALLEGRO_BITMAP* mpBMP;

	GraphicsBuffer(ALLEGRO_BITMAP* pTarget);

public:
	void init(string filePath);
	GraphicsBuffer();
	GraphicsBuffer(int width, int height);
	GraphicsBuffer(string filePath);
	GraphicsBuffer(GraphicsBuffer* pTarget);
	GraphicsBuffer(Color color, int width, int height);
	~GraphicsBuffer();

	int getBMHeight();
	int getBMWidth();
	ALLEGRO_BITMAP* getBMP();

};