#pragma once
#include <SFML/Graphics.hpp>
#include "Trackable.h"

class Color : public Trackable
{
	friend class GraphicsSystem;
	friend class GraphicsBuffer;
	friend class Text;
	friend class Sprite;

public:
	Color();
	Color(int r, int g, int b); //color at alpha 255
	Color(int r, int g, int b, int a); //color at given alpha
private:
	sf::Color mColor; //pretty much just an RGB color
};
