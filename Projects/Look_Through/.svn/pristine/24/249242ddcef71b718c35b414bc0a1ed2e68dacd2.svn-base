#pragma once
#include <Vector2D.h>

#include "SpawnEvent.h"
#include "GraphicsBuffer.h"

class SpawnCollectibleEvent : public SpawnEvent
{
private:
    Vector2D mPos;
    GraphicsBuffer* mpBuffer = nullptr;

public:
    SpawnCollectibleEvent(Vector2D pos, GraphicsBuffer* buffer);
    ~SpawnCollectibleEvent();

    Vector2D getPos() const { return mPos; }
    GraphicsBuffer* getBuffer() const { return mpBuffer; }
};