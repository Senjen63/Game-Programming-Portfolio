#pragma once
#include "Trackable.h" 
#include "Level.h"

class LevelManager : public Trackable
{
public:
	LevelManager();
	~LevelManager();

	void addLevel(string filename, Sprite wall);
	Level* getLevel();
	void setLevel(int level);
	int getSize();
	void cleanUp();


private:
	vector<Level*> mpLevelList;
	int mCurrent;
	int mSize;
};

