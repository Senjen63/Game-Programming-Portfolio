#pragma once

#include <string>
#include <unordered_map>
#include <PerformanceTracker.h>
#include <MemoryTracker.h>
#include "GraphicsBuffer.h"

const string ASSET_PATH = "..\\..\\common\\assets\\";

using namespace std;

class GraphicsBuffer;
typedef string BufferKey;

class GraphicsBufferManager : public Trackable
{
private:
	unordered_map<BufferKey, GraphicsBuffer*> mMap;
public:
	GraphicsBufferManager();
	~GraphicsBufferManager();

	void clear();

	void addBuffer(const BufferKey& key, string newBuffer);
	void addBuffer(const BufferKey& key, GraphicsBuffer* buffer);
	void removeBuffer(GraphicsBuffer* buffer);

	GraphicsBuffer* getBuffer(const BufferKey& key) const;

};
