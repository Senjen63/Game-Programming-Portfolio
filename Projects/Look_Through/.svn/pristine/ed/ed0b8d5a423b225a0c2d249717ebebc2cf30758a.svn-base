#pragma once

#include <Vector2D.h>
#include "SpawnEvent.h"
#include "GraphicsBuffer.h"

class SpawnWallEvent : public SpawnEvent
{
public:
    SpawnWallEvent(Vector2D, GraphicsBuffer*);
    ~SpawnWallEvent();

    Vector2D getPos() const { return mPos; }
    GraphicsBuffer* getBuffer() const { return mpBuffer; }
private:
    Vector2D mPos;
    GraphicsBuffer* mpBuffer = nullptr;
};