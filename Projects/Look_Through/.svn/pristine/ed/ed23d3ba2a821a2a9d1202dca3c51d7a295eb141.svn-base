#include "Level.h"

void Level::removeEmptyLocation(Vector2D vec)
{
	for (int i = 0; i < mEmptyLocations.size(); i++)
	{
		if (vec == mEmptyLocations[i])
		{
			mEmptyLocations.erase(mEmptyLocations.begin() + i);
			return;
		}
	}
}