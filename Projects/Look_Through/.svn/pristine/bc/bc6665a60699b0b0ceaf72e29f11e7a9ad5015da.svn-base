//BP
#pragma once
#include "HUDEvents.h"

class GameScore : public HUDEvents
{
public:
    GameScore(int playerScore);
    ~GameScore();
    int getPlayerScore() const { return mCurrentScore; }
private:
    int mCurrentScore = 0;
};