#pragma once

#include <Trackable.h>
#include <SDL_mixer.h>

#include "AudioSnippet.h"

class AudioSystem : public Trackable
{
public:
    AudioSystem() = default;
    ~AudioSystem();

    int play(const AudioSnippet& snippet, int repeatNum = 0);
    void stop(int channel);

    void stopAll();

    void init();
    void cleanUp();

private:
    bool mIsInitted = false;
};
