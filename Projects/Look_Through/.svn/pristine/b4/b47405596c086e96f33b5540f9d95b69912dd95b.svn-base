#pragma once
#include "GraphicsBuffer.h"
#include "Sprite.h"
#include "PerformanceTracker.h"
#include <vector>

class Animation : public Trackable
{
public:
	Animation( GraphicsBuffer* gbuff, float targetTime );
	~Animation();

	Sprite getCurrentSprite() { return mSprites[ mCurSprite ]; };

	void update( float deltaTime );
	void changeSpeed( int time );

private:
	int mCurSprite;
	float mElapTime, mTargetTime;
	vector<Sprite> mSprites;
	//Sprite* mpSpritesArray = nullptr;
};