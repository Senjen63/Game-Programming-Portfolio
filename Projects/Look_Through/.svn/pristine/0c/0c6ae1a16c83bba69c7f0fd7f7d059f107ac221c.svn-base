#pragma once

#include <unordered_map>
#include <vector>
#include <string>
#include <fstream>
#include <iostream>

#include <PerformanceTracker.h>
#include <MemoryTracker.h>

using namespace std;

typedef string WordKey;

class Language : public Trackable
{
public:
	Language();
	Language(string languageFile);
	string getCurrentLanguage();
	string getWord(string baseWord);
	void switchLanguage();

private:
	int mCurrentLanguage;
	vector<string> mLanguages;
	unordered_map<WordKey, string> mMap;

};