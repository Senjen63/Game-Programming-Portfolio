#pragma once
#include "Animation.h"
#include "Game.h"

class Unit : public Trackable
{
public:
	Unit(int x, int y, Animation* anim);
	~Unit();

	void update();
	void draw();
	void setAnimation(Animation* newAnimation) { mUnitAnimation = newAnimation; }
	void setPosition(int x, int y) { mX = x; mY = y; }
private:
	int mX;
	int mY;
	Animation* mUnitAnimation;
};
