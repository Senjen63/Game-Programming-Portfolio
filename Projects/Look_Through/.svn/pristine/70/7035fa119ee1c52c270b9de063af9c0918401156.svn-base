#include "Font.h"

Font::Font(const std::string& pathToFont, int fontSize)
{
	this->fontSize = fontSize;
	font = al_load_ttf_font(pathToFont.c_str(), fontSize, 0);
}

Font::~Font()
{
	al_destroy_font(font);
}
