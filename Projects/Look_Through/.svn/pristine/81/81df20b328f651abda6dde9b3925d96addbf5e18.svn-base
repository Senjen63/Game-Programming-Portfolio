#pragma once
#include<SFML/Graphics/Font.hpp>

#include <string>
#include <cassert>

#include <Trackable.h>

using namespace std;

enum Alignment
{
    ALIGN_LEFT = 0,
    ALIGN_RIGHT,
    ALIGN_CENTER
    
};

class Font : public Trackable
{
    private:
        sf::Font* mpFont;
        float  mFontSize;
        int mFlag;

        friend class GraphicsSystem;
        
    public:
        Font(string filePath, float fontSize, int flags);
        ~Font();

        float getSize();
        int getAlignment();
};