#pragma once

#include <EventListener.h>

class GraphicsSystem;
class UnitManager;
class Animation;
class Font;
class Game;

enum class MenuState
{
	INVALID_MENU = -1,
	MAIN_MENU,
	OPTIONS_MENU
};

using namespace std;

class MenuSystem : public EventListener
{
public:

	MenuSystem();
	MenuSystem(string filename, Font* menuFont);
	~MenuSystem();

	void update();
	void draw();

	void handleEvent(const Event& theEvent);

private:

	Font* mpMenuFont = nullptr;	
	GraphicsSystem* mpGraphicsSystem = nullptr;
	Game* mpGame = nullptr;

	MenuState mMenuState = MenuState::INVALID_MENU;

	int mButtonHeight = 192;
	int mButtonWidth = 72;

};

