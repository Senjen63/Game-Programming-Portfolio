#pragma once

#include "GameEvent.h"

class UpdateScoreEvent : public GameEvent
{
public:
	UpdateScoreEvent(int scoreChange, int segmentsChange);
	virtual ~UpdateScoreEvent();

	int getScoreChange() const { return mScoreChange; }
	int getSegments() const { return mSegmentsChange; }
private:
	int mScoreChange;
	int mSegmentsChange;
};