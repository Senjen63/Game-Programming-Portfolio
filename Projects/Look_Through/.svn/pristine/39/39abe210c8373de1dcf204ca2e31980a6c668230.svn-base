#pragma once

#include <vector>

#include <Trackable.h>

class Level;

using namespace std;

class LevelManager : public Trackable
{
public:

	LevelManager() {};
	~LevelManager() {};

	// Filenames need to be added in the order they appear in game
	void addFilename(string filename);
	void restartAtFirstLevel();
	Level* getNextLevel();
	Level* getCurrentLevel();
	

private:

	int mCurrentLevel = 0;

	vector<string> mvLevelFilenames;

};

