#include <PerformanceTracker.h>
#include "GraphicsBuffer.h"
#include "GraphicsSystem.h"
#include "Color.h"

class Font;
const int HUD_BUFFER_WIDTH = 420;
const int HUD_BUFFER_HEIGHT = 60;

class HUD : public Trackable
{
public:
	HUD();
	HUD(GraphicsSystem* pSystem, Font* pFont);
	~HUD();
	void init();
	void cleanup();
	void display();
	void setFPS(float FPS);
	float getFPS();
	void setTime(float time);
	float getTime();
	void setScore(int score);
	int getScore();
private:
	GraphicsSystem* mpGraphicsSystem = nullptr;
	GraphicsBuffer* mpScoreBuffer = nullptr;
	GraphicsBuffer* mpFPSBuffer = nullptr;
	GraphicsBuffer* mpTimeBuffer = nullptr;
	Font* mpFont = nullptr;
	float mFPS = 0.0;
	float mTime = 0.0;
	int mScore = 0;
	Color mWhite = Color(255, 255, 255);
	Color mBlack = Color(0, 0, 0);
};