//BP
#pragma once
#include "HUDEvents.h"

class Framerate : public HUDEvents
{
public:
    Framerate(float framerate);
    ~Framerate();
    float getFramerate() const { return mTrueFramerate; }
private:
    float mTrueFramerate = 0;
};