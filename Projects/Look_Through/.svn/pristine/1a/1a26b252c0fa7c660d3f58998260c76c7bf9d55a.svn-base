#pragma once
#include <Vector2D.h>

#include "Unit.h"
#include "SpawnEvent.h"
#include "GraphicsBuffer.h"

class SpawnSnakeEvent : public SpawnEvent
{
private:
    Unit* mpUnit = nullptr;

public:
    SpawnSnakeEvent(Unit* unit);
    ~SpawnSnakeEvent();

    Unit* getUnit() const { return mpUnit; }
};