#pragma once

#include "..\Unit.h"

class PieceManager;

class Queen : public Unit
{
private:
	bool mSelected = false;
	vector<Unit*> mpMovePts;
	vector<Unit*> mpAttackPts;

public:
	Queen();
	Queen(GraphicsBuffer* pBuffer, Vector2D location);
	~Queen();

	void cleanUp();
	void createMovePts();
	void draw(GraphicsSystem* pGraphicsSystem, PieceManager& team, PieceManager& otherTeam);
	void checkMovePts(Vector2D mousePos, PieceManager& team, PieceManager& otherTeam);
	bool checkAttackPts(Vector2D position, PieceManager& team, PieceManager& otherTeam, Vector2D& location);
	void resetPosition();

	void selected(bool selected) { mSelected = selected; };
	bool getSelected() { return mSelected; };
	unsigned int getNumAttackPts() { return mpAttackPts.size(); };
};