#pragma once

#include "Vector2D.h"
#include "Color.h"
#include "Font.h"
#include "Sprite.h"
#include <Trackable.h>

class Sprite;
class Font;
class Color;

class HUD : public Trackable
{
private:

	const std::string FONT_FILE = "cour.ttf";
	const std::string HUD_KEY = "hud";
	const int FONT_SIZE = 32;

	Color mTextColor = Color(255, 255, 255, 255);
	Color mBackpanelColor = Color(200, 50, 0, 255);

	Font* mpFont;

	std::string mLevelText;
	std::string mScoreText;
	std::string mFPSText;
	std::string mTimeText;

public:

	HUD();
	~HUD();

	void drawHUD();
};