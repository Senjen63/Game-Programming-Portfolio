#include "Font.h"

using namespace std;

Font::Font( string fileName, int fontSize )
{
	this->fontSize = fontSize;
	mpFont = al_load_ttf_font( ( ASSET_PATH + fileName ).c_str(), fontSize, 0 );
}

Font::~Font()
{
	al_destroy_font(mpFont);
}