#pragma once

#include "Trackable.h"
#include "Animation.h"
#include "GraphicsSystem.h"
#include "Vector2D.h"
#include <vector>

class Unit : public Trackable
{
	public:
		friend class UnitManager;
		friend class FoodManager;

		Unit(Animation anim);
		Unit(Animation anim, Vector2D pos);
		~Unit();

		void update(double dt);
		void updateSnake(double dt, Vector2D newDir);
		void draw(GraphicsSystem* graphSys);
		
		Vector2D getLocation();
		void setLocation(Vector2D loc);

	private:
		float mTimePerFrame;
		float mTimeLeft;

		Vector2D mLocation;
		Animation mAnimation;
};