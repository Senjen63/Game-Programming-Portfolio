#pragma once

#include <Trackable.h>
#include <EventSystem.h>
#include "Unit.h"
#include "SpawnFoodEvent.h"

class Food : public Trackable
{
    public:
        Food();
        ~Food();

        void init();
        void cleanup();

        bool getEaten() { return mEaten; }
        Unit* getCurrentFood() { return mCurrentFood; }

        void setEaten(bool);
        void setCurrentFood(Unit*);

        void updateFood();
        void endLevel();
private:
    EventSystem* mpEventSystem = nullptr;
    Unit* mCurrentFood = nullptr;
    bool mEaten = false;
};