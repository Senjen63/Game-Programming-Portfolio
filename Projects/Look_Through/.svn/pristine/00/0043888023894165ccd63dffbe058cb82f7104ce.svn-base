#pragma once

#include <SDL.h>
#include <Trackable.h>

class Color : public Trackable
{
public:
	friend class GraphicsSystem;
	friend class GraphicsBuffer;

	Color(unsigned char r, unsigned char g, unsigned char b, unsigned char a);
	~Color();
	unsigned char getRed() { return mR; }
	unsigned char getGreen() { return mG; }
	unsigned char getBlue() { return mB; }
	SDL_Color getColor() { return mColor; }

private:
	unsigned char mR;
	unsigned char mG;
	unsigned char mB;
	unsigned char mA;

	SDL_Color mColor;
	//SDL_Color getColor() { return mColor; }
};