#pragma once

#include "Trackable.h"
#include <unordered_map>

using namespace std;

typedef string TextKey;

class LanguageManager : public Trackable
{
private:
	unordered_map<TextKey, string> mTextMap;

	const string ENGLISH_MENU_FILENAME = "menu_text_english.txt";
	const string SPANISH_MENU_FILENAME = "menu_text_spanish.txt";
	const string FRENCH_MENU_FILENAME = "menu_text_french.txt";

	const string ENGLISH_HUD_FILENAME = "hud_text_english.txt";
	const string SPANISH_HUD_FILENAME = "hud_text_spanish.txt";
	const string FRENCH_HUD_FILENAME = "hud_text_french.txt";
public:
	LanguageManager();
	~LanguageManager();

	void init(int);

	string getText(const TextKey& key);
};