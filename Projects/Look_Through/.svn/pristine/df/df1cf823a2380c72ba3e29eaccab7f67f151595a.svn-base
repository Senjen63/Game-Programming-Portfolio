#pragma once
#include "UnitManager.h"

class Snake : public UnitManager
{

public:
	Snake();
	Snake(int speed);
	~Snake();
	void move(int direction, GraphicsSystem* display);
	bool isColliding();
	bool isColliding(Unit* unit);



private:

	Vector2D mDirectionalSpeed;
	int mDefSpeed;

};
