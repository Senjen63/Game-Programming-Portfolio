#include "Game.h"
#include "MemoryTracker.h"

using namespace std;

int main()
{
	gpGame = new Game();
	gpGame->init();

	gpGame->doLoop();

	gpGame->cleanup();
	delete gpGame;

	MemoryTracker::getInstance()->reportAllocations(cout);

	system("pause");
	return 0;
}