#pragma once
#include <Event.h>

enum GameEventType
{
    MESSAGE_EVENT = NUM_BASE_EVENT_TYPES,
    MOVE_EVENT,
    TURN_EVENT,
    INPUT_GAME_EVENT,
    ACTION_GAME_EVENT,
    SPAWN_GAME_EVENT,
    COLLISION_EVENT,
    END_LEVEL_EVENT,
    NUM_GAME_EVENT_TYPES
};

class GameEvent : public Event
{
    public: 
        GameEvent(GameEventType type);
        ~GameEvent();

    private:

};