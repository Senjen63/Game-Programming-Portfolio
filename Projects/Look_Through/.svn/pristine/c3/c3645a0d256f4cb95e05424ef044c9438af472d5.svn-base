#include "Sprite.h"

Sprite::Sprite(GraphicsBuffer* buffer, int x, int y, int height, int width)
{
	mX = x;
	mY = y;
	mHeight = height;
	mWidth = width;
	mpBuffer = buffer;
}

//Sprite::Sprite()
//{
//
//}

Sprite::~Sprite() {}

//void Sprite::createWallSprites(GraphicsSystem* mpGraphics, GraphicsBufferManager& mpgBufferManager, const string bufferKeyName, int height, int width)
//{
//	for (int i = 0; i < 10; i++)
//	{
//		string counter = to_string(i);
//
//		mpgBufferManager.createAndManageGraphicsBuffers(bufferKeyName + counter, (ASSETS + GREENSQUARE_FILENAME).c_str());
//
//		mpGraphics->flip();
//	}
//}