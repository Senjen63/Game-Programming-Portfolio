#include <iostream>
#include <string>
#include <cassert>
#include <PerformanceTracker.h>
#include <MemoryTracker.h>
#include <Timer.h>
#include <time.h>
#include <stdlib.h>
#include "GraphicsSystem.h"
#include "Game.h"

using namespace std;

int main()
{
	const int DISP_WIDTH = 800;
	const int DISP_HEIGHT = 600;
	gpGame = new Game(Vector2D(DISP_WIDTH, DISP_HEIGHT));
	gpGame->init();
	gpGame->loop();
	delete gpGame;
	MemoryTracker::getInstance()->reportAllocations(cout);

	system("pause");
	return 0;
}