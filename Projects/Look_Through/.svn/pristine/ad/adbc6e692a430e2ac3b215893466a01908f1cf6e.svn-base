#pragma once
#include <Trackable.h>
#include <string>
#include <Vector2D.h>
#include <SDL_image.h>
#include <SDL.h>
#include "Color.h"

using namespace std;

class GraphicsBuffer : public Trackable 
{
	friend class GraphicsSystem;
public:
	GraphicsBuffer(string path, SDL_Renderer* render);
	GraphicsBuffer(Color color, int height, int width, SDL_Renderer* render);
	GraphicsBuffer(SDL_Texture* pBitmap);
	~GraphicsBuffer();
	int getHeight();
	int getWidth();
private:
	//int mWidth, mHeight;
	bool mOwnsBitmap = true;
	SDL_Texture* mpBitmap;
	//GraphicsBuffer(ALLEGRO_BITMAP* pTarget);
};