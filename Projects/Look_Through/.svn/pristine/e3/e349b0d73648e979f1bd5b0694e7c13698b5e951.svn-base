//Evan Norzow, Nicolas Delbue, and Brandon Perry
#pragma once
#include <Trackable.h>
#include <iostream>
#include "Sprite.h"
#include "Font.h"
#include <SDL.h>
#include <SDL_image.h>

class GraphicsSystem : public Trackable 
{
	friend class Game;
private:
	int mWidth = 0, mHeight = 0;
	SDL_Window* mpWindow = nullptr;
	SDL_Renderer* mpRenderer = nullptr;
public:
	GraphicsSystem();
	GraphicsSystem(int width, int height);
	~GraphicsSystem();
	SDL_Renderer* getRenderer() { return mpRenderer; };
	int getWidth();
	int getHeight();
	bool init();
	void cleanup();
	void clear();
	void flip();
	void draw(Sprite sprite, int hori, int vert, float horiScale, float vertScale);
	void draw(GraphicsBuffer* pBackBuff, int hori, int vert, float horiScale, float vertScale);
	void displayMessage(int hori, int vert, string message, Color color, Font* font);
};