#pragma once
#include <Vector2D.h>
#include <PerformanceTracker.h>
#include <MemoryTracker.h>
#include "InputEvent.h"
#include <EventSystem.h>
#include "EventListener.h"
#include <SDL.h>
#include <string.h>
class InputSystem : public Trackable
{
private:
	//bool GetKey(int keyCode);
	int mMouseState;
	int mMousePosX;
	int mMousePosY;
	SDL_Event mEvent;
	EventSystem* mpEventSystem;
	void MouseDownEvent(SDL_MouseButtonEvent& m);
	void MouseUpEvent(SDL_MouseButtonEvent& m);

public:
	Vector2D GetMousePosition();
	bool GetMouseDown(int mouseCode);
	InputSystem();
	~InputSystem();	
	void Init();
	void CleanUp();
	void InputEventLoop();
	enum class MouseEvents
	{
		LMB_DOWN = 0,
		LMB_UP = 1,
		RMB_DOWN = 2,
		RMB_UP = 3,
		MMB_DOWN = 4,
		MMB_UP = 5
	};
	friend class Game;
};