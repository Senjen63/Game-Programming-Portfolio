#pragma once
#include <allegro5/allegro.h>
#include <allegro5/allegro_font.h>
#include <allegro5/allegro_ttf.h>
#include <allegro5/allegro_audio.h>
#include <allegro5/allegro_acodec.h>
#include <allegro5/allegro_image.h>
#include <allegro5/allegro_primitives.h>

#include <iostream>
#include <cassert>
#include <string>
#include "GraphicsBuffer.h"
#include "Font.h"
using namespace std;

class GraphicsSystem
{
private: 
	ALLEGRO_DISPLAY* mpDisplay;
	GraphicsBuffer* buffer;

public:
	GraphicsSystem(int width, int height);
	~GraphicsSystem();

	bool init();

	int getHeight();
	int getWidth();

	GraphicsBuffer* getBackBuffer();

	void draw(int x, int y, Sprite sprite, float scale);
	void draw(GraphicsBuffer* buffer, int x, int y, Sprite sprite, float scale);
	
	void writeText(int x, int y, Font* font, Color* color, string text);
	void writeText(GraphicsBuffer* buffer, int x, int y, Font* font, Color color, string text);
	
	void saveBuffer(GraphicsBuffer* buffer, string file);

	void flip();

};

