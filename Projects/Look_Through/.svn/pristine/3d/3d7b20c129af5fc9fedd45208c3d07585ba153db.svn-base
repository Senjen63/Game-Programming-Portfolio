#pragma once
#include <Vector2D.h>
#include "GraphicsBuffer.h"

class Sprite
{
private:
	GraphicsBuffer* mpGraphicsBuffer;
	Vector2D mLocation;
	int mWidth;
	int	mHeight;

public:
	Sprite();
	Sprite(GraphicsBuffer*& graphicsBuffer, Vector2D location, int width, int height);

	~Sprite();

	GraphicsBuffer* getBuffer();
	Vector2D getLocation();
	int getWidth();
	int getHeight();

};