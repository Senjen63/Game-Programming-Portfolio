#pragma once

#include "Trackable.h"
#include "Animation.h"
#include "GraphicsSystem.h"
#include "Vector2D.h"
#include <vector>

class Unit : public Trackable
{
	public:
		friend class UnitManager;

		Unit(Animation smurfAnimation, Animation deanAnimation);
		~Unit();

		void update(double dt, bool playAnimation);
		void draw(GraphicsSystem* graphSys);

		void setAnimation();
		void setLocation(Vector2D loc);
		Vector2D getLocation();
	/*	void setAnimationSpeed(int opt);*/

	private:
		Vector2D mLocation;
		int mCurrentAnimation;
		std::vector<Animation> mAnimations;
};