#pragma once

#include <Trackable.h>
#include <Event.h>
#include <string>

enum GameEventType
{
	INPUT_EVENT = NUM_BASE_EVENT_TYPES,
	MESSAGE_EVENT,
	POWER_UP_EVENT,
	DEATH_EVENT,
	NEXT_LEVEL_EVENT,
	NUM_OF_EVENTS
};

class GameEvent : public Event
{
private:

public:

	GameEvent(GameEventType eventType);
	virtual ~GameEvent();
};
