#include "Font.h"

using namespace std;

Font::Font(string filename, int fSize) 
{
	mFont = al_load_ttf_font(filename.c_str(), fSize, 0);
	assert(mFont);

	mFontSize = fSize;
}

Font::~Font() 
{
	al_destroy_font(mFont);
}