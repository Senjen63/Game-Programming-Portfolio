#pragma once

#include "SnakePart.h"

enum Direction
{
    Up,
    Right,
    Down,
    Left,
};

class Snake final : public Trackable
{
    SnakePart * mpHead;

    int mChildren;

    Direction mDirection;
public:
    Snake();
    Snake( int parts, const Color & color );
    ~Snake();

    void addChild();
    void draw( int squareSize ) const;
    [[nodiscard]] Direction getDirection() const;
    [[nodiscard]] Vector2D getPosition() const;
    void move(int width, int height) const;
    void setDirection( Direction dir );
    [[nodiscard]] SnakePart * getChild() const;
};
