#pragma once

#include <vector>
#include "Sprite.h"

class Animation
{
public:
	Animation(float timePerSprite, bool shouldLoop);
	~Animation();

	void addSprite(Sprite newSprite);
	void update(float deltaTime);

	Sprite getCurrentSprite() { return mSpriteList[mCurrentSprite]; }

	void changeSpeed(float newTimePerSprite);

private:
	std::vector<Sprite> mSpriteList;
	int mCurrentSprite = 0;

	float mTimePerSprite = 0;
	float mTimeRemaining = 0;

	bool mShouldLoop = false;
};