#pragma once

#include <vector>
#include "SDL.h"
#include "SDL_Mixer.h"
#include <Trackable.h>

class Sound : public Trackable
{
private:
	std::vector<Mix_Chunk*> mpSounds;
	int mNumSounds = 0;
	bool soundPlaying = true;
public:
	Sound();
	~Sound();
	void AddSound(std::string path);
	void PlayAudio(int ID);
	void PlayAudio(int ID, int loop, int channel);
	void ToggleAudio(bool audio);
};