#pragma once

#include <Trackable.h>
#include <map>

class GraphicsBuffer;

using namespace std;

class GraphicsBufferManager : public Trackable
{
public:

	GraphicsBufferManager() {};
	~GraphicsBufferManager();

	GraphicsBuffer* getGraphicsBuffer(string key);

	void addGraphicsBuffer(GraphicsBuffer* buffer, string key);
	void addGraphicsBuffer(string fileName, string key);
	void addGraphicsBuffer(int width, int height, string key);
	void deleteGraphicsBuffer(string key);
	void clearGraphicsBuffer();

private:

	map<string, GraphicsBuffer*> mpGraphicsBufferMap;
	
};

