#pragma once
#include "Animation.h"
#include "GraphicsSystem.h"

class Unit : public Trackable
{
public:
	Unit();
	Unit(int x, int y, Animation* smurf);
	Unit(int x, int y, Sprite sprite);
	Unit(int x, int y, Animation* smurf, Animation* dean);
	~Unit();

	void update(float removeTime);	
	void update(int x, int y);
	void draw(GraphicsSystem* display, Vector2D pos);
	void drawSprite(GraphicsSystem* display, Vector2D pos);
	void storeAnimations(Animation* smurf, Animation* dean);
	void setAnimation();
	void changeSpeed(float timeChange);
	Vector2D getPos();
	Sprite getSprite();
	Animation* getAnim();
	void setPos(Vector2D pos);
	void pause();


private:
	Animation* mpSmurfWalk;
	Animation* mpDeanWalk;
	Animation* mpCurrent;
	Sprite mSprite;
	int mX;
	int mY;
	int mAnimChoose;
};
