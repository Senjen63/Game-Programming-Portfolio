#pragma once

#include <Vector2D.h>
#include "GameEvent.h"

class EndLevelEvent : public GameEvent
{
public:
    EndLevelEvent(bool);
    ~EndLevelEvent();

    bool getNextLevel() const { return mNextLevel; }
private:
    bool mNextLevel = false;
};