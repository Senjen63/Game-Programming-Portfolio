#include "Font.h"

Font::Font(string path, int size)
{
	mSize = size;
	mpFont = al_load_ttf_font(path.c_str(), size, 0);
}

Font::~Font()
{
	al_destroy_font(mpFont);
}

float Font::getSize()
{
	return mSize;
}