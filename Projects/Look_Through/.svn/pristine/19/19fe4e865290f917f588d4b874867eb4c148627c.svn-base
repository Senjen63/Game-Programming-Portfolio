#pragma once
#include "Unit.h"
#include "InputSystem.h"
#include <PerformanceTracker.h>
#include <vector>

using namespace std;


class UnitManager : public Trackable
{
public:
	friend class Unit;

	UnitManager();
	~UnitManager();

	void cleanup();

	void addUnit(Vector2D mousePos);
	void deleteUnit(Vector2D mPos);

	void clear();

	void updateAll(float time);
	void updateTime(float time);
	void drawAll(GraphicsSystem* pSystem);

	void changeLast();

	void pauseUnits();

private:
	vector<Unit*> mpUnits;
	int mNumUnits;
};