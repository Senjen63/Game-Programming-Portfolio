#pragma once

#include "Trackable.h"

#include <SDL.h>
#include <string>

using namespace std;

class Color;

class GraphicsBuffer : public Trackable
{
	friend class GraphicsSystem;
	friend class Sprite;
	private:
		const string FILE_PATH = "buffer_data.txt";

		SDL_Surface* getSurface();
		SDL_Surface* mpSurface;
		const string ASSET_PATH = "../GalahadAssets/art/";
		
		int mInt1;
		int mInt2;
	public:
		GraphicsBuffer(int bitmapWidth, int bitmapHeight);
		GraphicsBuffer(string fileName);
		GraphicsBuffer() {};
		~GraphicsBuffer();

		int getWidth() { return mpSurface->w; }
		int getHeight() { return mpSurface->h; }
};