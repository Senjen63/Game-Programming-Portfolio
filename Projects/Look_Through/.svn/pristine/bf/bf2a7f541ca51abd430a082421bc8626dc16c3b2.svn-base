#pragma once

#include "Trackable.h"
#include <unordered_map>

using namespace std;

typedef string TextKey;
typedef string IntKey;
typedef string DoubleKey;

class OptionsManager;

class DifficultyLoader : public Trackable
{
private:
	unordered_map<TextKey, float> mTextMap;
	unordered_map<IntKey, int> mIntMap;
	unordered_map<DoubleKey, double> mDoubleMap;

	const string EASY_FILENAME = "easy_diff_data.txt";
	const string NORMAL_FILENAME = "medium_diff_data.txt";
	const string HARD_FILENAME = "hard_diff_data.txt";

public:
	DifficultyLoader();
	~DifficultyLoader();

	void init(int);

	float getFloat(const TextKey& key);
	int getInt(const IntKey& key);
	double getDouble(const DoubleKey& key);
};