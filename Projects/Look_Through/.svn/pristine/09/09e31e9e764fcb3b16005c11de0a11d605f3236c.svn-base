#pragma once
#include <string>
#include <trackable.h>
#include <Event.h> 

enum GameEventType
{
    MESSAGE_EVENT = NUM_BASE_EVENT_TYPES,
    INPUT_EVENT,
    MOUSE_CLICK_EVENT,
    UI_EVENT,
    NUM_GAME_EVENT_TYPES
};

class GameEvent : public Event
{
private:
    GameEventType mGameEventType;
public:
    GameEvent(GameEventType type);
    ~GameEvent();
    GameEventType GetEventType();
};