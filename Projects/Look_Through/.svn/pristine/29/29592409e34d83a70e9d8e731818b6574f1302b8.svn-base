#pragma once
#include "allegro5/allegro.h"
#include "Color.h"
#include <string>

using namespace std;

class GraphicsBuffer
{

	friend class Unit;
	friend class GraphicsSystem;

private:
	ALLEGRO_BITMAP* mpBitmap;

	int mHeight;
	int mWidth;

	ALLEGRO_BITMAP* getBuffer();

public:
	GraphicsBuffer(string assetPath, string filename);

	GraphicsBuffer(int width, int height);

	GraphicsBuffer(Color color, int width, int height);

	~GraphicsBuffer();

	int getHeight();

	int getWidth();
};